# Systematic AI：超越端到端模型的智能体系结构

*副标题：为什么 AI 的下一次跃迁来自体系结构，而非更大参数的单体模型*

## 0. 被误读的度量衡

进入 2026 年后，AI 业界与学术界共同撞上了一个尴尬且反常的现实：在各类代理人任务（Agentic Tasks）的基准测试中，**Harness**——即负责分发工具、维护上下文并决定何时中断的脚手架系统——不再只是外围的“胶水代码”或可被忽略的实验噪音，而是演变成了一个能够实质性颠覆评测结果的决定性独立变量。

最初的裂痕来自一线工程团队的微观实验。Stencil进行的一组工程实验给出了一个令人不安的结果：他们没有训练任何模型，没有增加任何参数，只修改了 AI Agent 修改代码文件的工具接口（从String replace改成了hashline），就让 16 个主流模型在编码任务上的总体表现平均提升 15 个百分点。

在应用工程界，这种“外围决定上限”的现象正在迅速蔓延。LangChain发现，他们在基于同一个模型（`gpt-5.2-codex`）的Terminal Branch 2.0测试中，光是靠优化外围的Harness基础设施（System Prompt，Tools，Middleware），就成功让它在tb2测试中的得分从52.8%提升到了66.5%，全球排名从Top30提升到了Top5。LangChain在文章中更是直接提出了"Harness Engineering"的概念，指”把模型天然不稳定、尖峰式的能力，塑造成针对具体任务可靠工作的系统能力”。他们在后续的调优当中发现，每个模型对System Prompt，Tools，Middleware，Context Management，Memory等要素的敏感度都大不相同，需要为其专门定制Scaffold Profile。DigitalApplied上一篇关于SWE-Bench上的报道也指出，同一个模型，在使用模型厂商定制的Scaffold和使用第三方Scaffold时，模型在SWE-Bench Pro上的表现会有很大的差异；尤其是模型厂商自己报告的结果（往往使用闭源定制脚手架）和第三方使用通用脚手架复现的公开结果，差异尤其巨大。

而最近的一件事情则更说明了一个有趣的事实——GPT-6 Astra发布时，声称其在ARC-AGI-3这个Benchmark上的通过率达到了99.9%；但当别人试图用OpenAI的API+自己的Scaffolding框架来复现这个结果时，他们发现得分只能在17%-63%之间浮动。除了说明OpenAI对外提供的API的模型智能水平可能不如内部版本（俗称“降智”）以外，还可能说明，OpenAI为其定制的闭源脚手架可以在如此惊人的程度上影响AGI Benchmark的结果。

学术界也注意到了这些现象，2026年已经有多篇学术论文讨论相关问题：

- arXiv:2603.13285开启了整个讨论。虽然还没有上升到Harness这一层，但他们研究的课题——“如果输入表达方式轻微变化，模型输出能力就发生明显变化，那么 Benchmark 测到的是模型能力，还是 prompt distribution？”，已经成为了整个体系的第一片乌云。他们指出，传统的评测依赖固定Prompt、干净输入和标准模板，实际上可能会高估模型在真实工作负载下的鲁棒性。不过这篇论文还只是在探讨输入本身，并没有上升到Harness这一层。
- arXiv:2605.27922正式把LangChain等提到的Harness Engineering摆上了台面。论文中明确指出，Agent = Model + Harness，而Harness包含了模型完成任务需要的一系列基础设施，比如Context Management，Tools，State，Constraints，Permissions，Tracing，Recovery。论文设计了106 个 sandbox tasks、5194 条执行轨迹和多模型、多 harness 配置，观察不同模型的表现。他们发现，同一个模型构成的不同model-harness对在任务完成度、过程质量、效率和失败行为上产生了显著的差异。论文最终得出结论：“智能体的能力应当在模型与测试框架（model-harness）的配置级别进行报告，而不应归功于基础模型本身。”
- arXiv:2607.22585开始攻击现有Benchmark榜单的"模型中心"倾向。他们认为，公开排行榜往往只写明了模型名称和得分，而忽略了Harness层——工具提供、上下文管理和停止策略——对于模型表现的影响，并没有标明。他们测试了`Qwen 3.6 Plus`和`MiniMax M2.5`这两个模型，使用了`Goose`、`OpenCode`和`OpenHands-SDK`三个Harness脚手架框架。结果他们发现：不同脚手架的选择可以让解决同一个任务的Token消耗量差上40倍；同时，不同的脚手架也呈现出了截然不同的失败指纹特征，比如Goose的REASON Failure，OpenHands的VERIFY/MAX TURNS和OpenCode的idle-loop/TIME。论文指出，Harness框架不仅影响了模型的性能表现，它实际塑造了Agent的行为模式。
- 而最新的arXiv:2609.09218则把问题提高到了一个更根本的层级。它提出了一个灵魂之问——“Benchmark是否正在度量一个错误的对象？”。论文提出了“双重测量混淆“这个概念，并给出了两个混淆来源——一个是“执行关键决策实际由Scaffold完成，而不是模型”，比如工具选择、停止策略、执行策略等等；另一个则是“计分器本身也在记录错误的东西”，比如表面的格式和真实的任务正确性是完全两码事。论文建议后续的benchmark score应该伴随scaffolding level、scoring criterion、reliability level等指标一同解读，才能得到真实数据。

这些密集浮现的工程裂痕与学术反思，共同指向了一个无法继续回避的理论盲区。

长期以来，整个AI研究界和产业界都在遵循这么一条极度简单、甚至是线性的度量哲学：由一个客观公正的Benchmark对不同的AI Model进行测试，得到其智能的体现——也就是Benchmark分数。

在这套世界观里，Benchmark 被近似地当作一台稳定、可比、相对无偏的测量仪器，Model 则是被放置在实验环境中的待测样本，而跑出来的分数则被直接等同于“模型在实际任务情境下表现出的智能水平”。至于外围的代码、提示词模板、工具调用的 schema 转换以及上下文截断逻辑，在传统认知中被统统归类为“测量仪器上的灰尘”——不过是些无足轻重的胶水代码，理应在实验控制中被尽量固定，作为“常量”而非“变量”。

然而，现在的Harness实际上已经远不是当年一个简单的薄API调用了。现在的Harness Engineering，已经把原本的“胶水代码展开成了包括上下文管理（Context Management）、工具调用（Tools）、状态流转（State）、规划重试（Agent Loop）、分工调度（Dispatching & Multiagent）、验证仲裁（Verification）、停止判定（Termination）的复杂系统。他们实际上承载了很多智能的功能，实际上从上面这些证据来看，也很能影响模型在真实工作负载下的表现。很多时候，改进Harness结构带来的工程任务性能改进甚至已经超过了改进模型本身带来的收益。

当把文件编辑工具换成hashline就能带来十几个点的性能表现，当一套精细的定制脚手架能把模型在Benchmark中的表现放大10-20%，当不同的Harness策略决定了Agent的工作流是会顺畅完成还是陷入无法逃出的死循环的时候——我们就不得不回答一个问题了：如果Harness本身给Agent的实际表现带来的影响力相当于甚至超过了Model的基础能力，直接决定了模型是否能完成实际的工程任务，那么继续把Harness视作模型外围的“胶水”和“辅助设施”，是否有点鸵鸟了呢？

如果把算力的消耗、策略的生成、状态的维护和最终的成败，全数归功于那个仅仅负责在局部生成概率分布的语言模型，无异于在计算机体系结构中把整台服务器的 I/O 吞吐、进程管理与任务调度，统统记在 CPU 算术逻辑单元（ALU）的某一组晶体管账上。Harness 就不再是模型外围的附属品，它在物理上、在信息流上，都已经成为了和Model并驾齐驱的、实际完成计算的智能系统的主体部分。

模型依然强悍，但光靠一个裸模型已经很难在实际生产中落地。对于实际处理工程问题而言，一个强力Harness的辅助和管理是不可或缺的。换言之，我们或许一开始就在度量一个错误的对象。

那么，Harness 究竟只是模型能力的放大器，还是已经成为智能本身的一部分？要回答这个问题，我们必须先理解它是如何从“胶水代码”一步步膨胀成今天的执行系统的。

## 1. Harness的崛起与抽象的裂痕

要接着往下走，我们得先回答一个至今没有认真回答过的问题：**Harness 到底是什么？**

试着给它下一个定义，你立刻会发现这件事出奇地难。它不是模型，也不是应用；它同时包含工具 schema 这样的接口定义、"失败之后重试几次"这样的策略、"上下文超长时该砍掉哪一段"这样的内存管理，以及一句写在 System Prompt 里的"请仔细检查你的答案"。没有任何一份规范规定过它的边界，因为**它从来不是被设计出来的，而是被攒出来的**。

所以本章不打算给 Harness 编年，只想回答一个更硬的问题：它为什么长成现在这个样子。

答案朴素得有点难看：**它的每一个组件，都是为修补模型的某一处具体失能而出现的，而且都是在工程现场的痛点上被逼出来的。因此它的形状是缺陷的形状，而不是问题的形状。**

但形状可疑不等于方向错误。把规格、能力、控制、状态逐层从语义核心搬出去，这个分解本身是对的——那恰恰是一个通用语义处理器应有的分工。真正的毛病在于：这些职责被搬出来了，却没有被安置在任何地方，只能在应用层代码里各自流浪。

看清这一点，最好的实验室是软件开发。它的真值便宜且无歧义——编译过不过、测试绿不绿，不需要人类充当裁判；它的经济压力最大，所以补丁来得最早、最猛；而它又恰好是结构最显式的领域，类型系统、语法树、编译器、测试套件就摆在那里。也就是说：**在结构最唾手可得的领域里，我们造出了一套代价最高的结构替代品。** 先记住这句反讽，第三章会来收账。

### 1.1 规格层：把任务说明白（Prompt Engineering）

第一次翻车发生在最没有技术含量的地方。人们发现同一个模型、同一个任务，只是换一种说法，成功率就会明显跳变。权重没变，任务没变，变的只是"任务被表述的方式"。

这说明模型并不拥有一份独立于表述的任务规格——**任务规格只能存在于上下文里**。于是有了 Prompt Engineering：System Prompt 规定角色与纪律，模板固定输入形态，少样本示例提供一个不更新权重就能改变行为的通道，格式约束尽力驯服输出的外形。它的本质是一次职责转移：**规格从代码里的类型与断言，搬进了自然语言。** 少样本示例尤其值得留意——它等于把上下文当成了权重更新的替代通道：既然改不动模型，就往它的工作记忆里塞几个例子，用有限的上下文位置换一次近似微调。

这条路的代价是结构性的，不是调参能解决的。**规格一旦写成自然语言，就同时获得了最大的表达力与最小的一致性**：没有类型、没有签名、没有校验器、没有编译期。任务与它的规格从此强耦合在同一段文本里——改任务必改提示词，换模型必重写提示词。这正是序章里"每个模型都要单独定制 Scaffold Profile"的来源。

而这一层里最有意思的组件，是反思式提示——"先检查你的答案再输出"、"验证一遍"。它表面上是一句规格，实质上已经不是规格了：它做的事情是**让系统多跑一轮**。人们第一次在这里引入了"用一句自然语言换一个额外执行回合"的手法。换句话说，**控制流的最原始形态，是伪装成提示词出现的**。这一点在 1.3 会长成一个真正的循环，但现在只需记住：A 层在自己最朴素的工具里，已经埋下了 C 层的种子。

### 1.2 能力层：把世界接进来（Tools）

第二类翻车要贵得多。模型会信心十足地调用一个不存在的函数，会算错三位数乘法，会写出一段看起来完美但跑不起来的代码，会对文件系统里真实存在的状态一无所知。一句话：**它在语义空间里很强大，在物理世界里是瞎的。**

补丁的第一形态相当直白——既然模型听得懂话，就用话教它调工具：在提示词里描摹每个工具的用途、参数与格式，再在输出侧用正则表达式和 try-catch 把它偶尔写坏的结构捞回来。它确实能工作，但它把一个本该是接口问题的事情，降格成了文本解析问题。而接下来的三个阶段，是这条弧线上的三次不同选择：

- **原生 Function Calling**：把调用语法从提示词里搬进协议。模型不再需要"学会"怎么写调用，语法成了接口的一部分。
- **Structured Output**：把约束从"输出之后"搬到"解码之中"。约束解码让非法结构在物理上无法被生成。
- **MCP 一类协议**：试图把"工具长什么样"从各家私有的约定，变成能够互相通告的标准。

其中 Structured Output 值得单独标记，因为**它是我们这份清单里唯一一次选对了地方的修补**。其他所有组件都在事后努力解析模型的输出；只有它反过来改动了生成过程本身，把一条运行时不变量推回了基底——**与其祈祷它输出合法，不如让它无法输出非法。** 请记住这个形状：正确的修补，总是发生在约束本该生效的那一层。

MCP 的位置则要微妙一些。它标准化的是传输，不是语义：它规定组件之间怎么说话，却没有规定谁在指挥、状态归谁管、失败由谁负责。所以它是一个症状而非解药——**行业已经感觉到这里需要一个契约层了，但仍然是在 Model-Centric 的框架里寻找它。** 这句话我们会一直用到第六章。

B 层留下了两处残渣。第一，调用意图依然由概率模型生成，因此永远需要被解析——协议能规范语法，不能保证意图。第二，副作用一旦发生，状态就分裂成两半：一半在模型看得见的上下文里，一半在沙盒与真实文件系统里，而没有任何机制保证两者一致。第三章会把这两处残渣连本带利算一遍。

### 1.3 控制层：谁来决定下一步（ReAct Loop）

有了工具，一个新的问题立刻浮出来：**谁决定下一步做什么？**

一次模型调用只能吐出一个动作。但真实任务需要"看一眼结果，再决定下一步"——而这个"再决定"，恰恰是任何单次前向都给不出的东西。于是人们把调用包进一个循环里：观察、思考、行动，再观察、再思考、再行动。这就是 ReAct 的骨架。它随后长出了分工更细的零件——Planning 把"计划"和"执行"拆开，好让整体路线先定下来再逐条推进；Validating 在动作之后插入一道检查，判断这一步是否真的奏效；Retrying 允许失败后重来；Stopping 负责判断"什么时候算做完了"。

这一层的性质，和前面两层完全不同。1.1 和 1.2 都只是在**给模型补充它能用的东西**：补充规格、补充能力。而 1.3 补的不是能力，是**控制流**。

这是整章真正的转折点。在此之前，模型是"被调用者"——代码把输入递给它，等它返回，然后由代码决定接下来怎么办。到这一刻起，**循环的循环变量变成了模型的解码结果**：`while` 的判据不再是程序员写下的布尔表达式，而是模型生成的下一段 token。控制平面，第一次从确定性的代码，搬进了一个由浮点概率驱动的黑盒里。

这是本节最重要的一句话，值得停一下：**我们不是给模型加了一个外设，我们是把系统的方向盘交了出去。** 之后两章里所有关于"脱轨""死循环""成本失控"的讨论，根都在这里，而不是在某个框架的实现质量上。

五个零件之中，**Stopping 最值得单独说，因为它是全清单里最"系统"的一个，也最反讽**。终止语义本该是运行时的一等原语——它需要独立于被调度者、需要能被外部强制、需要可预测。可它在这个阶段被实现成了什么呢？要么是循环里的一个 `if`，判断模型这次输出里有没有出现某个标志字符串；要么更糟，被写成 System Prompt 里的一句礼貌请求："如果你认为任务已经完成，请只输出 DONE。" 也就是说，**我们把一个本该由内核裁决的问题，外包成了一次对模型的协商。** 请记住这个错位——第八章的原语一，整个都是冲它来的。

Control 层留下了两处残渣，都会在第三章被清算。第一，**失败与成功都不可归因**：任务跑崩的时候，你很难说清是模型选错了动作、工具返回了意外结果、还是循环本身没有正确的退出条件。第二，**成本失去了可预测的上界**：一次调用的开销是可以预算的，一个由解码结果驱动的循环却可以烧到你叫停为止。序章里那篇论文提到的三种失败指纹——有 Agent 卡在 REASON 上，有 Agent 撞到 MAX TURNS，有 Agent 陷入 idle-loop 直到超时——不是三家框架各自的 bug，而是**同一种体系结构缺陷在三种不同实现里的三个投影**。

### 1.4 状态层：把它搬出去，代价才开始显现（Context & Memory）

前三层解决的都是"这一步怎么做"，第四层面对的则是"记不记得住"。任务一旦变长，两个天花板会同时撞上来：上下文窗口有限，而跨会话的连续性为零。

于是有了三样东西。

**上下文压缩与修剪**：既然装不下，就丢掉一部分。丢的方式五花八门——总结前文、保留最近若干轮、按重要性裁剪——但它们有一个共同性质：**有损，且不可逆**。你无法在事后判断刚才那次摘要吃掉了哪一条关键信息，因为它已经不在了。状态管理本该是精确的记账，在这里退化成了有损的抽样。

**外部记忆**：既然上下文装不下，就搬到外面存。方向是对的，但搬出去的东西缺了三样最基本的东西——**没有地址**（没有一个稳定的 handle 让计算节点引用某一条具体记忆）、**没有生命周期**（没人知道一条记忆何时该失效）、**没有权限**（没有机制规定哪个计算单元可以读写哪一部分）。所以它名义上是"记忆"，实际形态更接近一堆可以用相似度捞取的文本碎片。

**Multiagent**：既然一个模型忙不过来，就分几个。分工的动机是合理的——不同子任务本就该由不同的角色、不同的上下文来处理。但它立刻带来一个更根本的问题：这几个 Agent 之间怎么通信？答案说出来有点荒凉——**用自然语言字符串**。状态不再是可寻址的共享内存，而是互相之间来回传递的散文。

到这里，四层走完，可以给出一个结构性的观察了：

**我们一层一层地把它剥干净，最后剩下的核心只负责生成 token。而这个位置恰恰是对的——语义生成本来就该是一个通用基底干的事。问题在于，剥下来的那些东西，没有被安置在任何地方。** 规格、能力、控制、状态，全都散落在应用层的业务代码里，各自以最方便的形式临时存在，彼此之间没有契约。

这就是"抽象的裂痕"真正的位置。它既不在模型里，也不在任何一个具体框架的实现里，而在**这两者之间那片没人认领的空白**。

### 1.5 Skills：胶水第一次有了名字

在这一堆补丁里，Skills 是个异类，值得用一小节单独收尾。

它做的事情看起来最不起眼：把一组提示词和对应的工具打包成一个可以分享、可以复用的格式。但它带来的变化是别的组件都没有带来的——**它让一段 Harness 配置第一次拥有了名字、边界和版本**。

在此之前，那套"提示词 + 工具定义 + 重试策略"始终是某个应用内部的私有代码，散落在仓库各处，无法被引用、无法被比较、无法被讨论。一旦它被命名、打包、分发，性质就变了：**它从"某段看不出来的业务逻辑"，变成了一个可以被人指着说"就是它"的一等产物。**

这一拍的价值不在复用，而在**可见性**。胶水一旦有名字，就再也无法被当作不存在。而这恰好和序章里 LangChain 那句 "Harness Engineering" 形成了同一件事的两种说法：一边是工程界在实践里给这批东西起了名字，一边是这篇文在体系结构层面指出——**它们被命名了，却仍然没有被安置。**

### 1.6 编年史错位：这不是设计，这是堆积

讲到这里，必须处理一个史实上的别扭，因为它本身就是论据。

四层的顺序是清楚的：规格、能力、控制、状态，从外到内。但组件真正问世的顺序是跨层的：先是提示词工程，然后就有了 ReAct 那样的循环——也就是说，**行业先学会了让模型转圈，很久以后才给它配上一份标准的接口契约**（原生 Function Calling 与 MCP 都晚于循环的普及）。原因不难理解：让模型转圈，靠几句提示词就能 hack 出来；而标准化一个接口，需要跨厂商、跨生态的协调。**痛点的紧迫程度，和解决的难度成反比——于是人们总是先解决容易修的，哪怕它的抽象层级更靠下。**

这构成了整章最有说服力的一段论证：

**组件的发明顺序，与它们在一套干净的体系结构里本该占据的层级顺序，并不一致。痛点是按"哪个最疼"排的，不是按依赖关系排的。** 所以我们先有了一个会转圈的循环，很久以后才有终止语义的讨论；先有了上下文里的拼装状态，很久以后才有人问"状态归谁管"。

换句话说：**这批东西从来没有一个设计者。** 它们每一件都是某个人在被某个具体问题咬住时，用当时手上最方便的工具做出的最直接反应。而一堆各自正确的最直接反应叠在一起，并不等于一个系统——它只是恰好能跑。

### 1.7 岔路口

四层清点完毕，可以给出总结：**规格、能力、控制、状态——这四样恰好构成一个系统运行所必需的完整职责集合。** 前两者在语义核心之外，后两者更靠内核；这个分解本身是健康的，甚至是必然的。不好的是它们现在各自为政、层级混乱、没有契约。

于是顺着序章的证据，我们可以给出一个可操作的判据，用来判断 Harness 是否已经具有计算显著性：

$$\Delta(\text{Model A} + \text{Harness}_A,\ \text{Model A} + \text{Harness}_B) > \Delta(\text{Model A},\ \text{Model A}_{\text{next-gen}})$$

当"换一套脚手架"带来的收益超过"换一代模型"带来的收益时，Harness 就不再是实验里的扰动项，而是决定结果的自变量之一。这不是比喻，是可以直接测的量——序章里那些实验数据，量的就是它。

现实已经走进了这个阶段，并且有一个恰当的称呼：**Model–Harness System**。它是一个正确的事实描述，描述了我们此刻真实在用的东西。

但事实描述不是体系结构。Model + Harness 这个式子里，加号两边的地位是不对等的：模型是一个有明确边界的计算基底，而 Harness 是一堆没有边界的约定俗成。把它们写在一起，只是承认了它们同时存在，并没有说明它们之间该有什么关系。

所以，面对补丁累积到足以决定成败的今天，有且只有两条路：

**(a) 这些补丁被模型自己吸收掉**——下一代基础模型足够强，把循环、路由、记忆统统内化进权重，Harness 收缩回一行薄薄的 API 调用。

**(b) 这些补丁被抽象出来**——承认规格、控制、状态本就属于比"一次前向"更高的层级，为它们在应用与模型之间搭起一个真正的运行时层。

这两条路的取舍，正是本文接下来要论证的对象。我们的立场是 (b)，但论证不能靠断言：它需要先看清端到端范式究竟在哪些地方付出了不该付出的代价（第三章），再理解为什么"通用性"从来就不排斥分层（第四章）。在那之前，**Model + Harness 只是一个正在忍受的现状，而不是一个已经想清楚的答案。**

## 2. 端到端假设的黄金时代

第一章末尾，我们把"补丁被模型自己吸收"列为两条出路之一。这一章的任务，是替那条路说话——而且要说得比它自己的支持者说得更好。

理由很简单：如果一条路线只能被它最弱的版本打败，那这场辩论就没有价值。真正值得认真对待的对手，是那个由**正确推理**得出的错误结论。它不会自己暴露，因为它的每一步都站得住。

所以本章不追溯谁说过什么，也不代任何人立论。我们只做一件事：把"一个单体模型可以吞下一切"这句话，还原成一个可以被检验的推论——它的公设、它的证据，以及它从未言明的那个前提。

### 2.1 统一接口：一切皆序列

这套范式最容易被误读的地方，是它看起来像一个关于**规模**的主张。其实它首先是一个关于**拒绝分解**的主张。

图像是序列，音频是序列，代码是序列，动作是序列，连"想一步"也是序列。于是三种统一随之成立：**接口统一**（万物进出皆为 token，没有类型，没有模式，只有流）、**学习统一**（不管任务是什么，优化目标都是同一个下一 token 的交叉熵）、**算子统一**（不管计算的是什么，落到硬件上都是同一批矩阵乘法）。

这套方案真正的哲学内核在于：**任何被人为切分出来的结构——语法树、类型、图、状态机——都不被认为是机器必需的部件，而被降格为一种可选的表示形式。** 机器唯一需要的东西是序列，以及足够多的参数去拟合序列里的一切规律。

必须说清楚它为什么有道理，否则我们会低估它。因为在历史上，人为切分确实经常失败：语言学家手写的语法规则被统计模型碾过，专家设计的特征被端到端网络碾过，精心构造的知识库在纯文本语料面前显得又贵又慢。每一次预先切分，都可能切掉模型本来自己能发现的东西。**在那种历史背景下，拒绝分解不是偷懒，而是当时唯一能保住"通用"这个承诺的选择。**

但它在这条路上带了一个东西：一个从未被写下来的前提——**能表述，就意味着能廉价地计算。** 我们知道句法树可以写成文本，可我们没说清写完之后的代价。这句话是一颗雷，第三章来踩。

> **钉子：把一切结构压平成序列，在当时不是偷懒，而是唯一诚实的选择。**

### 2.2 权重即结构：把结构吸收进参数

第二条公设是：结构不一定需要被显式地写出，它可以被隐式地拟合。

这就是参数吸收率的来源。语法规则、世界常识、常识之间的高阶关联、跨领域的类比模式，全都没有以任何显式形式存在于系统里，却都能在稠密权重中被逼近。你找不到一条规则，但模型行为符合规则；你指不出哪个参数保存了"巴黎是法国的首都"，但问它它答得出。**结构没有消失，它只是被溶解了。**

这是 E2E 最漂亮的一场胜仗，而且它是真的。

问题出在传播过程中。这场胜利在流传中变成了另外一句话——**"结构是一种不必要的假设"**。而它实际证明的只是**"结构可以被隐式地吸收到权重里"**。这两句话看起来几乎一样，差别却是本文全篇要处理的那个盲点。

差别在于：被吸收的结构，和被写出的结构，是**不同性质的资产**。写出来的结构是有名字、有边界、可被寻址、可被验证的；吸收进权重的结构是一团统计上的倾向，它只在概率意义上成立，取用它需要一次昂贵的采样，而且你不能指着它的任何一部分说"就是这里"。

吸收之后，结构不再有名字。

这也不是全然的坏消息——无名结构换来的是极限的柔性，这正是 E2E 在开放语义空间里无可替代的地方。代价只是被推迟了而已：它带上了第二个未言明的前提——**学会了，就意味着能廉价地取用。** 同样是一颗雷，第三章见。

> **钉子：它没有被证明为错，它只是被证明为可以被吸收——而后来的十年里，这两件事被当成了同一件事。**

### 2.3 规模即能力：无限膨胀与边际递减

前两条公设说的是"可行"，第三条说的是"必然"：拟合质量随算力与数据单调提升。

它作为经验定律是稳固的——账面上，投入更多算力与数据，下游表现确实持续改善。这也是整个时代最有效的单一信念：它把一个方向上的所有局部不确定性，压缩成了一句可以反复下注的话。

但"单调提升"这个词里藏着两件事，都必须说清楚。

**第一件：需求的膨胀没有上界。** 提升来自更长的上下文、更多的采样、更宽的搜索、更大的批量，而这三样东西消耗的算力和显存是指数式上涨的。这里的账尤其不好看：上下文的显存开销（KV Cache）随上下文长度线性增长，随并发批量再乘一遍——而上下文长度恰恰是 Harness 最爱用的手段。第一章里那条"把状态塞进上下文"的策略，直接决定了底座的存储需求会一路膨胀下去。**应用层省下来的抽象功夫，最后是被底座的显存账单付掉的。** 这笔账我们第三章会正式清算。

**第二件：边际效用递减。** 性能随算力的提升大致服从幂律——换算下来就是，每次把算力翻十倍，收益在刻度上只是加了一小截，而且是越来越小的一截。这条曲线不是"某一天会撞墙"，而是"从第一天起就在变缓"。再叠加数据侧的现实：高质量语料是有限的资源，不是可以无限追加的变量。

于是我们看到了一件非常有意思的事：**业界从来没有停止用工程手段去延缓这两条曲线。** 而且手段相当漂亮——

- **混合专家（MoE）**：把"总参数量"和"每 token 实际计算量"解耦，用稀疏激活换来更大的知识容量。
- **注意力机制的层层改良**：从多头注意力到分组查询、多查询、潜在注意力，再到各种稀疏化与线性化，目标是压掉二次复杂度里那些收益最低的项。
- **深度复用**：像 looped Transformer 这类设计，让同一批权重被反复过一遍，用计算换参数。
- **KV Cache 的治理**：分页、共享、量化、淘汰，把第一章里那笔存储债尽量做小。
- **架构的重新划分**：因果编解码结构的重切分、预填充与解码的分离，把两种截然不同的计算形态分给不同的执行路径。

现在请注意这批手段的共同点，因为这是本节真正的论点：**它们全部都是体系结构层面的干预。** 每一件都不是在提高模型的"智能"，而是在为它买喘息的空间——这恰恰承认了"通用计算是昂贵的"这条本文的核心前提。

但同样请注意它们的第二个共同点：**这些干预全部发生在模型的边界之内。**

MoE 让底座自身变成了内部的异构系统；注意力改良让同一个前向里的计算分布变得不均匀；深度复用让同一批参数承担不同的角色。也就是说，行业已经**完全接受了"靠结构换效率"这件事**——只是它只允许自己在权重内部这样做，一旦越过模型边界，最有效的那个手段（把不同负载分给不同性质的计算单元）就完全消失了。

**所有体系结构的智慧都被花在了模型内部。模型外部因此一直是荒原。** 这句话是第三章第二节（意外操作系统）的真正根因：不是工程师不会设计系统，而是过去十年里，每一点结构性的聪明都被吸收进了权重，没有一分为外部抽象层留下来。

至于那两条曲线能不能一直被这样推迟下去，本章不作答。第四章会带着计算史的证据回来。

> **钉子：它成功了太久，久到它掩盖了自己不适用的那些地方。**

### 2.4 经验检验：模型到底吃掉了什么？

到目前为止，前三条公设都是"推理"。现在应该看看事实：这十年里，端到端基底实际吃掉了哪些曾经属于 Harness 的职责？

| 已经被基底吃掉                         | 仍然滞留在应用层               |
| -------------------------------------- | ------------------------------ |
| 工具调用的语法约束（受约束解码）       | 终止语义：何时算做完           |
| 输出格式的解析与容错（结构化输出）     | 跨任务、跨会话的持久状态与地址 |
| 少样本示例的格式模仿（上下文学习能力） | 失败归因与可重放               |
| 短程上下文拼接（长上下文窗口）         | 成本上界与预算控制             |
| 浅层检索式记忆（内化知识 + 长上下文）  | 多组件之间的协调与仲裁         |

这张表左边是真的在缩短。第一章里我们列过的那些组件，有相当一部分正在从 Harness 收缩回基底——这也是本章最需要主动承认的一点，因为不承认这一点的辩护根本不可信。

但表里的分界线不是随机的。**请把左右两列各看一遍，然后问一个问题：它们的差别是什么？**

差别是：**被吃掉的每一项，都能在一次前向内部闭合；没被吃掉的每一项，都必须跨越多次调用，或者跨越多个组件。**

- 语法约束、格式解析、示例模仿，都是在**同一次生成**里就能完成的判断——让解码器不要吐出非法结构，让模型在一个上下文里学会模仿一种格式，这些都是"这一次前向之内"的事。
- 终止语义不是。它要判断的是"这一整轮循环该不该结束"，而这个判断的对象在时间上位于生成之后。
- 状态地址不是。它要描述的是"那条信息放在哪里"，而这个"哪里"必须在多次调用之间保持稳定。
- 成本上界不是。它要给一整段跨调用、跨组件的执行过程设一个外部约束。

所以"Harness 会不会消失"这个问题应该被改写：**不是会不会消失，而是哪一部分会消失。**

会消失的，是那些本该由基底承担、也确实可以由基底承担的职责——它们的归位是好事。不会消失的，是那些**在原理上**不能在一次前向内部闭合的职责：不是"模型还不够强"，而是"这件事的性质不在单次生成的射程里"。上下文再长，吃掉的是"拼接"，吃不出"地址"；模型再强，吃掉的是"格式约束"，吃不出"终止语义"。

**单次前向的边界，就是执行层能力的自然边界。** 这句话是本章最值钱的一笔遗产，第七章它会变成"语义协处理器"这个定位的技术依据。

我们也在这里得到了一个可用的尺子。判断某个职责将来会不会被基底吸收，不需要预测下一代模型的参数量，只需要问一句：**它能不能在一次前向内部闭合？**

> **钉子：单次前向的边界，就是执行层能力的自然边界。**

### 2.5 第四假设：那条从未被明说的前提

到这里，三条公设都已经站住了。现在让我们尽最大努力，替这条路说话——用它的第一人称，说它最强的版本：

> 如果注意力可以在一次前向里表示任意关系，如果稠密权重可以吸收任意结构，如果更多算力总还能换到更好的拟合，那么把控制、状态和验证也交给它，不过是在把同一件事做得更彻底一些。为什么要为它们另立一套机器？那套机器里的每一条规则，都只是我们还没有足够的算力去拟合的临时替代品。

这段话里没有一步是错的。它只是**悄悄地多带了一条前提**。

那条前提从来没有被写出来，因为它看起来不像是需要论证的东西。它大概是这样的：**学习与执行，服从同一套法则。**

前面三条回答的是"如何获得智能"——面对未知的、非凸的、开放的高维语义空间，无偏见的通用算力与自发拟合是最优手段。这一点本文完全同意，并且认为它是这十年最重要的方法论成就。第四条则把这三条**沿用到"如何部署智能"上**：既然通用手段是获得智能的最优手段，那么它也应当是组织智能的最优形态。

这就是范畴错误发生的确切坐标：**一整套关于"探索"的定理，被当成了关于"执行"的规范。**

具体差在哪里？在探索阶段，我们面对的是我们尚不知道规律的东西，所以任何预先的结构都可能是错的，必须让机器自己去发现。而在执行阶段，我们面对的是**已经被发现、已经被锁定、而且高频重复**的东西——排序、类型检查、图遍历、事务、状态迁移。这些地方的规律不再是待发现的，它们已经被发现了，甚至已经以符号形式写在了某处。此时让系统每一次都把已经确定的事情重新映射回连续概率空间里掷一次骰子，这就不是在坚持通用学习，而是纯粹的浪费与退化。

所以本文的核心命题之一定在这里：

> 端到端模型是不可思议的强大计算基底，**但我们可能正在强迫一个本应处于执行层的通用部件，去承担属于更高体系结构层级的职责。**

这里第一次出现了"层级"这个词。需要事先声明：**层级的正当性不能靠类比来证明。** 说"计算机是这样做的所以 AI 也该这样做"，只是一句修辞；真正的问题在于，分层究竟是人为强加的分类癖，还是复杂性增长到某个程度后的必然产物。第四章会用计算史本身来回答它。

> **钉子：端到端不是错误的方向。它只是被赋予了一个不属于它的职责范围。**

### 2.6 收束

三条公设各自带着一条从未被检验的前提，可以并排列出了：

| 第二章的公设             | 未经验证的隐含前提       | 前提失效之处               | 第三章对应代价   |
| ------------------------ | ------------------------ | -------------------------- | ---------------- |
| 表述公设                 | 能表述 = 能廉价计算      | 结构被压平后失去地址与索引 | 3.1 表征税       |
| 学习公设                 | 学会了 = 能廉价取用      | 取一小点知识要唤醒全部参数 | 3.3 通用性税     |
| 规模公设                 | 结构干预只发生在权重内部 | 外部没有任何抽象层承接职责 | 3.2 意外操作系统 |
| **同一性假设（未明说）** | 学习法则 = 执行法则      | ——                         | 贯穿全章         |

这张表就是第三章的目录。注意第三行与第四行的关系：前三行是**技术层面**的账，第四行是前三行的**总账**——如果学习与执行确实服从不同法则，那么前面三条前提的失效就不是可以靠工程慢慢修补的偏差，而是同一处结构性错位的三种表现。

下一章不去论证端到端错了。它只做一件事：把这三笔被算漏的账，连本带利算一遍。

## 3. 端到端范式的结构性代价

第二章结束时，桌上摆着三张价签：能表述不等于能廉价计算，学会了不等于能廉价取用，结构干预止步于模型边界。这一章要做的事很单纯——把这三笔账各自算一遍。

算账有个好处：它不需要立场。所以本章给自己立一条纪律，每一节都回答同样三个问题——**这笔代价的本体是什么？它记在哪个物理量上？为什么它不会自己消失？**

第三个问题最关键。工程上的昂贵本身构不成论点，一个"贵"字在市场上通常撑不过两年；只要有利可图，再贵的东西也会被优化掉。只有当一笔代价**在结构上**无法被优化掉，它才值得被写进一篇讨论体系结构的文章里。

也先把本章不做什么说清楚：它不主张回到符号主义，不主张削减通用计算，也**不打算论证分层是必然的**——那是第四章的事。这一章只做一件事：把账算清，把证据摆好。

### 3.1 表征税：信息无损，可计算性有损

先拆掉一个常见的错误说法。有人认为序列化会让模型"丢掉"结构信息——这是不成立的。从信息论的角度来看，一份图、一棵语法树、一张关系表、一台有限状态机，都可以被严格无损地写成一维 token 流，重建它所需的一切都在那串 token 里。

所以准确的说法是：**序列化保留的是信息，丢失的是结构。** 信息一个都没有少，少的是访问它的方式。

丢掉的具体是三样东西。

| 丢失的属性     | 符号系统里的形态                   | 压平之后的形态                               |
| -------------- | ---------------------------------- | -------------------------------------------- |
| **地址**       | 指针、主键、句柄；一次跳转即可定位 | 只能靠扫过整段上下文来定位                   |
| **确定性语义** | 类型、外键、协议约束；可被机器校验 | 退化为统计倾向，可能出错，也可能错了而不自知 |
| **拓扑**       | 边与邻接关系是显式的、可数的       | 需要靠注意力在一次前向里重新推断             |

这三样东西有一个共同点：**它们都关于"访问"，而不关于"内容"。** 内容还在，代价只是转移了——转移到每一次问它"在哪里"的时候。

现在把这个代价写下来。

| 操作                        | 符号系统                   | 压平后交给序列模型                           |
| --------------------------- | -------------------------- | -------------------------------------------- |
| 取第 n 个节点               | O(1) 指针跳转              | O(n) 上下文扫描，并伴随注意力的二次开销      |
| 校验类型是否兼容            | O(1) 表查找，不兼容即报错  | 一次概率推断；可能出错，而且出错不报错       |
| 遍历含 V 个节点、E 条边的图 | O(V+E)                     | 随节点数二次增长的注意力，外加逐节点的解码步 |
| 约束"全局不得重名"          | 一次全局检查，成立即被保证 | 无保证，只能依赖模型自己记得                 |
| 事务回滚                    | 幂等原语，语义明确         | 无对应原语，只能重新生成一遍                 |

这张表要说的不是"哪种实现更优雅"，**它是复杂度的差别，而不是常数项的差别。** 一次指针跳转和一次上下文扫描之间，隔着一个复杂度类；而工程优化擅长改善常数，不擅长跨越复杂度类。

这一点在序章的那些实验里其实已经露过头。同一个任务，在不同脚手架下的 token 消耗可以差到几十倍——这种量级的差距无法用"模型更聪明"来解释，它更像表征方式与访问路径带来的成本差异。同样地，长上下文的评测里反复出现过一个现象：关键信息在序列中处于什么位置，会显著影响它被取用的概率。**注意这个现象的形状——它不是"记不住"，而是"找不到"。** 而这正是失去地址之后的必然表现。

比单步成本更麻烦的是误差的传播方式。这里有一个容易滑过去的要点：**模型不是在"处理"结构，而是在"重建"结构，而重建的每一步都是一次采样。** 处理是确定的，重建不是。于是误差会沿着结构本身传播：结构越深、依赖越密，一次早期的偏差被后续步骤继承并放大的机会就越多。这解释了一个一线工程师都熟悉的现象——短小的修改往往干净利落，而涉及深层依赖链的长程改动则容易连带崩坏。**它不是模型"变笨了"，是重建的深度变大了。**

这里可以得到本章的第一条判据。由于重建代价与结构的刚性成正比，我们可以把任务按刚性排开：

**散文 / 意图 < 配置 < 代码 < 语法树与类型系统 < 协议与状态机 < 关系型数据**

越靠右，结构越确定、约束越硬、可校验性越强，压平后的重建代价也越高；越靠左，结构越柔性、歧义越多，通用基底的隐式拟合就越有优势。**这条谱系不是修辞，它是一条可以直接用来做计算路由的依据**——第五章会回来用它，第八章的第一条原语也建立在它上面，此处先记下。

但在离开本节之前，必须说清一件容易被我自己的论证带偏的事：**在刚性谱的柔性端，压平恰恰是正解。** 语义理解、意图解析、模糊检索、开放域的关联——这些地方的"结构"本来就是不确定的，硬要显式化只会更差。本节要求的从来不是用符号系统替代通用基底，而是**让不同的负载去到不同的地方**。这个"分流而非替代"的立场，本章会反复出现，因为它是整个论证的承重墙：一旦它被误读成"回到符号主义"，后面所有结论都会变形。

> **钉子：结构一旦被压平，访问它的代价就从一次跳转涨成一次扫描——而这是复杂度层面的涨幅。**

### 3.2 控制税：每个 Agent 都在手搓一个低配操作系统（重写版）
第一章已经讲过这批组件是怎么来的，这里不再重述。本节要回答的是一个不同的问题：**为什么这种"每个应用各自重造一套调度、状态与容错"的局面会持续存在，以及它的代价记在谁头上。**

原因在第二章其实已经露出来了。过去十年，**所有体系结构层面的智慧都被花在了模型的边界之内。** 混合专家把总参数量与实际计算量解耦，注意力机制的层层改良压掉了二次复杂度里收益最低的部分，深度复用让同一批权重反复过一遍——它们全部都是结构性的干预，行业也早就完全接受了"靠结构换效率"这件事。只是这份接受有一个不成文的前提：**它只允许发生在权重内部。** 一旦越过模型的边界，最有效的那一招——把不同性质的负载交给不同性质的执行单元——就彻底消失了。

也不是没有人在边界外努力，只是那是一片荒原。没有坐标系，也没有公共道路，于是每个团队都在自己那一小块地上重新开垦一遍。这就解释了为什么每个 Agent 应用里都躺着一套结构相似、却互不相识的东西：

| 操作系统原语 | 现代 AI 应用里的对应物       |
| ------------ | ---------------------------- |
| fork / exec  | 再发一次 API 请求            |
| 调度器       | "让模型自己决定下一步"       |
| 共享内存     | 不断拼接的上下文             |
| 系统调用     | 约束解码 / 结构化输出        |
| 权限位       | 系统提示里的一句"请不要……"   |
| 崩溃转储     | 日志里一段难以复现的自然语言 |

这张对照表很容易被读成一句抱怨，**但它真正的重点在最后两行。** 操作系统之所以是操作系统，从来不是因为它提供了"调度器"或者"内存管理"这些具体功能——而在于它**内核的不变式有专门的守卫者**。一个用户态程序可以任意写错、可以越界访问、可以在任何时刻崩溃，但它无法改写内核的数据结构；同样重要的是，它可以**在任何时刻被强制中断、被抢占、被杀死**，而这件事不会损坏系统本身。

所以操作系统最根本的贡献不是功能，而是一种资格：**在它之下，一个组件被允许失败，被允许中断，而不牵连整个系统。** 我们不妨把这个资格叫做**特权级**。

现代 AI 应用最深的裂缝恰恰在这里：**模型既是用户态，又是内核。** 它执行约束，因此它也可以覆盖约束；它是被执行的一方，因此没有人有资格打断它。没有特权级，就没有不变式的守卫者，一切约束都只能退化为协商——这正好解释了第一章留下的那个悬案：终止语义为什么会被写成一句写在提示词里的礼貌请求？**不是因为它难，而是因为缺少特权级，所以除了请求别无他法。**

这就是本章交付的第二条判据：**特权级**——组件是否能在不破坏系统不变式的前提下失败、中断与重启。它会在第六章被用来判断 Agent Framework 与运行时之间的真正区别，也会在第八章成为第一条原语的直接依据。

那么这笔账记在谁头上？可以用两项来记，它们分别沿一条轴的两端展开。

**重造税（横向）。** 每个团队、每个项目都在重写一遍同一批东西：调度、重试、压缩、超时、容错。这笔工程量是可以被估算的，而它的特殊之处在于，**它不产生任何可复用的公共资产**——一百个团队写一百遍，总量是一百份，不是一百倍的价值。

**累积税（纵向）。** 这一项最不显眼，也最贵。需要先说清它不是什么：现代 Harness 并不缺乏可见性——每一步的工具调用、每一次上下文修剪、每一轮重试都被完整记录下来，序章里那篇论文甚至能从数千条执行轨迹中提炼出不同脚手架的失败指纹。从外部重新配置同样完全可行：换一个工具、改一段提示词、调一下重试策略，都不需要动模型。序章里那组"只改文件编辑接口就换来十几个点"的实验，就是一次教科书式的成功干预。

正因为这两件事都成立，剩下的那件事才更值得注意：**问题不在于看不见，而在于看见之后留下的东西留不下。**

一次调优所得的经验，有效期绑在一个具体的三元组上——这个模型、这套脚手架、这类任务。换掉其中任何一项，经验同时失效。序章里"每个模型都需要单独定制一套脚手架配置"的观察就是这笔账：不是没有人总结出规律，而是总结出来的每一条规律都不能跨配置使用。

于是这个领域呈现出一种奇特的状态：**工具前所未有地透明，行业却依然在很大程度上健忘。** 没有人是瞎的，问题是看见的东西没有变成资产。一条"在这个模型上这样调会更好"的经验，几乎无法被抽象成可迁移的命题，因为它从未说明自己为什么有效——它只是一次成功的观察。

两项税的区别也值得点明：**重造税说的是代码无法复用，累积税说的是知识无法沉淀。** 前者其实还好办，代码至少可以被复制；后者更难缠，因为你要复制的不是一份文件，而是一条带条件的判断。

顺带留一句给第八章：**所谓"外部重配置"，今天是手工的、一次性的，它还没有成为可以被声明、被版本化、被继承的产物。** 累积不了的东西，通常正是那些没有被当作一等对象的东西。

最后需要指出本节与其他两节在性质上的差异，因为它解释了一个看起来别扭的错位：3.1 与 3.3 是**技术性**代价，而 3.2 是**组织层**代价。它对应的不是某条技术前提，而是第二章的规模公设——**它是 scaling 成功的副产品，不是某项技术的缺陷。** 这一点值得为身处其中的工程师说清楚：荒原上长满手搓的系统，不代表造这些系统的人不懂体系结构，只代表他们所在的那一层从来没有得到过投资。序章那些证据里没有一条能归咎于工程能力不足——它们全部呈现为同一种形状：**每一处都在局部做对了，整体却没有形状。**

至于这片荒地上后来又长出了什么——各种脚手架、各种编排框架——第六章会正面处理。此处只留一句：**那是荒地上的另一种开垦方式，不是修路。**

> **钉子：我们缺的不是更聪明的胶水，而是那个本应禁止胶水越权的东西；不是看不见，而是留不下。**

### 3.3 通用性税：信息密度失配

先把本节与上一节的分界线划清，否则它们会被读成同一件事：**3.1 说的是结构被压平之后的代价，3.3 说的是即使结构完好无损，执行者本身也过于通用。** 前者是表征问题，后者是机制问题。

设想一个极端的例子：给一组数据排序。答案是确定的，给定输入之后没有任何不确定性可言。但在当前的体系里，完成它的方式是唤醒一个通用网络做一次完整前向——一个条件熵接近于零的步骤，占用的却是一次通用推理的全部开销。

把这件事写成一个可测量的定义：**通用性税 = 参数唤醒量 ÷ 该步实际新增的信息量。** 这个比值不需要精确的数字就可以看出量级的不对称：一边是确定性步骤本身所携带的信息量，另一边是一次前向所调动的整个网络。本节不给具体数值，因为它关心的是这个比值的存在与其不可消除性，而不是它此刻等于多少。

真正要命的不是"浪费算力"这一层。**通用性税最深的代价是它制造了不可判定性。** 一个确定性任务一旦交给概率机制执行，它的正确性就无法从内部得到保证——答案拿到手之后，你并不知道它是算对的还是碰对的。于是唯一的补救办法是从外部再验证一次，而验证本身又是一次同样昂贵的通用前向。这笔税因此是**复利**的：计算一次、验证一次、失败再重来一次，而三者的成本在同一量级上。

这里必须正面处理一个最可能被提出的反驳：混合专家的稀疏化不是已经把这个问题解决了吗？

没有，而且理由很具体。**路由的是参数子集，不是计算范式。** 无论激活哪一部分专家，一个确定性步骤依然要走完一次完整的前向——稀疏化降低的是"每次前向动用了多少参数"，而不是"这件事该不该走前向"。何况路由本身还要付出代价：路由的判据开销、路由判断错误带来的不确定性、以及整个路径可解释性的丧失，都是新加进来的成本。所以结论是：**稀疏化缓解了通用性税的绝对值，却没有提供"把已经确定的负载交给非神经网络单元"的能力。** 而缺的恰恰是后者——一个能把负载投向不同性质计算单元的调度层。

由此得到本章的第三条判据：**信息密度**。它与 3.1 的刚性谱是一对：**刚性强、信息密度低的负载，是最该被分流走的；柔性、高信息密度的负载，才该留给通用基底。** 两条判据合起来，就构成了第五章那张"系统级效能"图背后的路由依据——什么该送到哪里，这一章给出的就是算法。

同样地，这里也要把立场摆正：本节不是说一切事情都该符号化。探索性的、模糊的、开放域的任务上，通用基底依然是唯一可行的选择，甚至可以说，通用性税正是为这份通用性支付的、完全值得的保费。问题只出在那些**早就没有不确定性、却被反复按通用计算处理**的步骤上。**要的是分流，不是取代**——这句话在 3.1 出现过一次，此处以不同的理由再次出现，因为它确实是全部论证的承重墙。

> **钉子：我们缺少的不是更强的通用计算，而是一个把它送到该去的地方的调度层。**

### 3.4 总账：一笔错位的三种记法

三笔账算完，可以把它们归到一起了。

它们的形态差别很大——一个是访问方式的退化，一个是组织方式的缺失，一个是分配方式的不当——但最终都落回同一本物理账上：**算力消耗、内存搬运、端到端延迟、失败率，以及为取得一个可信结论所必须付出的重复次数。** 这是它们共同的货币，也是为什么它们必须被放在一起算，而不能被当成三个独立的工程问题逐个击破。

因为它们是耦合的，而且互相放大：

```
      控制平面靠上下文维持
              ↓
      上下文必须越来越长
              ↓
      表征税按二次方上涨 ──┐
                          │
   状态没有地址，只能靠    │
   把结构压平来传递 ───────┤
                          ↓
              通用性税无法被摊薄
         （确定性步骤依然唤醒整个网络）
```

这张图值得停一下：**三条边上的每一处，都同时是另一处的成因。** 因此它们不存在"先修哪一个"的解法——你不能通过优化上下文管理来消灭表征税，因为上下文之所以长，是因为控制平面和状态本来都住在它里面；你也不能靠提高模型能力来消灭通用性税，因为"该不该走通用前向"这个问题与模型强弱无关。**不能逐个修的代价，只能整体换层级。**3.1 与 3.3 的代价可以靠更好的表征、更好的分配来缓解，而 3.2 的代价没有这种缓解路径：**一个没有被抽象出来的经验，不会因为算力变得更便宜就自动变得可迁移。** 它是三笔账里唯一一笔不会随技术进步而自然缩水的。

于是所有分支汇到同一个根上：第二章那条从未被明说的前提——**学习与执行服从同一法则。** 如果它成立，这三笔账就只是暂时的不便，可以用更好的工程和更大的模型慢慢磨平；如果它不成立，那这三笔账就是同一处错位在三种不同场景下的投影。

由此可以给出本章最实用的一条推论，它正面回答了那句最常听到的话——"再等一代模型就好了"：

**把模型做大，不会解决这三笔账，反而会放大它们。** 更大的模型加重通用性税；更长的上下文按二次方加重表征税；而无论它长到多大、多快、多强，它都不会凭空产生一个特权级，因此也就不会有人有权中断它。**这三笔账里没有任何一笔是关于"能力不足"的，它们全是关于"位置不对"的。**

但必须在这里划清边界。本章证明了代价存在，**没有**证明分层是必然的。从"这里有代价"到"必须换一套层级"之间，还隔着一条独立的论证：分层究竟是人为强加的分类癖，还是复杂性增长到某个程度后无法回避的产物？不能靠类比回答，只能靠证据回答。

那是第四章的任务。

## 第四章：从 Bitter Lesson 到异构演进

第三章末尾挂起了一笔债：它证明了统一执行有代价，却没有证明分层是必然的。从"这里有代价"到"必须换一套层级"之间，还缺一条论证。这一章就是来还这笔债的。

先说清本章的出身。本文关于"把不同性质的负载交给不同性质的计算单元"的全部主张，都不是从计算机借来的比喻，而是**异构计算这套既有逻辑在 AI 场景下的直接延伸**。所以本章不是一次绕道，它是全文的源头。它要交代的是这套逻辑的完整形态——包括它是怎么被逼出来的、它的收益从哪来、以及它为什么**不会自动兑现**。

也因此，本章给自己立两条规矩。

第一，**机制优先于历史**。先立起"为什么必然分层"，再用历史去检验，而不是反过来。历史只能充当验证材料，不能充当论证本身。

第二，**类比必须被显式降格**。说"计算机这样做，所以 AI 也该这样做"只是一句修辞；类比能提供的只是怀疑的资格，不能提供结论的资格。

下面按四拍走：一条单轴撞墙，于是转向结构；结构带来异构，异构带来协调；协调有它的定律，而定律指出收益在哪里兑现。

### 4.1 Bitter Lesson 的适用域

讨论这件事绕不开那句话，而绕不开它的方式不是驳倒它，是精确划定它的管辖范围。为了不让本章变成稻草人，先把它的最强版本写出来：

> 在目标能被一个标量度量、解的搜索空间开放、失败可以承受、通用算力可以持续扩张的场合，靠搜索的通用方法终将胜过人类预先设定的切分。

请留意这句话里**没有**什么东西。它没有说"通用计算也是执行的最优形态"。它从一开始就只是一条**关于搜索的经验定律**。

而它列出的那四个条件，在两侧的成立情况完全不同：

| 条件             | 探索侧：发现规律                     | 执行侧：复现规律                                       |
| ---------------- | ------------------------------------ | ------------------------------------------------------ |
| 目标可被标量度量 | 成立：损失、似然、奖励信号           | 失效：正确性、可复现性、可中断性、成本上界彼此不可通约 |
| 搜索空间开放     | 成立：规律未知，任何先验都可能是错的 | 失效：规律已被锁定，没有可搜之物                       |
| 失败可承受       | 成立：代价是训练算力                 | 失效：代价是一次生产事故，且不可回滚                   |
| 通用算力可扩张   | 成立：更多算力换来更好的拟合         | 失效：更多算力只放大第三章那三笔账                     |

四条条件里有三条是"目标不同"，这已经够用了。但这里还有一层更本质的差别，值得单独立出来，因为它比"执行不等于学习"要精确得多。

**搜索的燃料是廉价评估。** 搜索要能进行下去，前提是能给候选答案廉价地打分——不能打分就无从比较，无从比较就没有搜索。在探索阶段，这个打分器唾手可得：一个损失函数、一个奖励信号、一个基准分数。而在执行阶段，它不存在。已经确定的答案没有可搜索的余地；而那些尚未确定的答案，验证一次正确的成本与生成一次的成本处在同一个量级——这正是第三章那笔"验证的复利税"在说的事情。

于是可以给出一个比"执行阶段不一样"更结实的表述：

> **执行阶段不靠搜索，不是因为通用计算变弱了，而是因为它的燃料不存在。** 当验证一次正确答案的代价等同于生成一次答案时，用采样去逼近确定性答案，在经济上已经破产。

至于编译器最终胜过手写汇编，那是这条定律在探索侧的一次完美验证，这里不必展开。

需要立刻划清边界：本节完全不适用于开放域任务。写一份小说、做一次科学假说生成、在模糊的需求里找方向——这些地方依然处在探索侧，这条定律的效力一分未减。本章主张的从来不是按领域分流，而是**按负载分流**。

> **钉子：Bitter Lesson 说的是"在你还不知道规律的时候，不要猜"；它从没说过"在你已经知道规律之后，还要假装不知道"。**

### 4.2 分层的三条机制

现在来立机制。这里要找的是三条**与计算机无关、只与复杂度有关**的压力，然后检查它们是否在别处同样成立。

**机制一：互操作成本的平方增长。** 设系统里有 M 种上层形态与 N 种下层实现。若没有契约层，每一对都需要互相适配，代价随 M×N 增长；一旦在中间插入一层稳定的契约，代价降为 M+N。这不是工程审美，是一条组合数上的算术。而它恰好就是第三章那片"荒原"的代价公式：每个团队各自写一遍适配，等价于让 M×N 全额展开。

**机制二：目标的不可通约。** 一个执行者无法同时最优地服务多个不可通约的目标。通用基底在训练时被优化的那个目标（序列似然的拟合质量），与它在系统里被要求的那些目标（正确、可复现、可中断、成本有界）**根本不是同一个函数**。而且它们无法被合成一个标量——不存在中立的加权方式，任何加权都需要有人来定，那就等于把决策权交还给了系统设计者。只要被要求的目标维度超过一个，分解就是唯一的出路。

**机制三：物理异质。** 不同计算形态的物理性状本就不同：串行深度与并行吞吐不可兼得，算力与带宽不可兼得，确定性输出与采样输出不可兼得。这里有两个纯粹的案例。一个是**存储层次**——没有人把内存做成一个平坦的大平面，因为访问代价相差几个数量级；缓存不是人类的美学偏好，是物理后果。另一个是**加速器本身**——同一片硅上，吞吐与延迟、带宽与算力在结构上互斥，所以每一件加速器都是**按其代价剖面切出来的**。

但机制三需要一句更关键的定性，因为它决定了本章能不能回应最可能的一条反驳：**异质不必是硬件事实，它首先是代价剖面的差异。** 在 AI 系统里，这个差异同样是数量级与复杂度类上的：确定性求解与采样生成的单位成本之间、刚性与柔性负载的重建代价之间（3.1 已经算过）、串行深度与并行吞吐之间。物理异质只是代价异质最显眼的一种特例，而不是它的定义。

三条机制有一个共同特征：**它们都不问"你是什么系统"，只问"你的复杂度到了没有"。** 所以只要一个系统同时具备（互操作的种数很大、目标多维且不可通约、不同负载的物理代价相差数量级），分层对它就是同一个结论。**我们借用的是机制，不是形态**——这一点必须说死，因为它是本章唯一的免疫层：即便后文引用的全部历史都被读者否认，这一节依然立着。

| 机制           | 未成立时会发生什么                   | 本文对应哪一条主张          |
| -------------- | ------------------------------------ | --------------------------- |
| 互操作平方增长 | 每个应用自建一套适配（第三章的荒原） | 第六章契约层 / 第八章原语三 |
| 目标不可通约   | 单一标量分数掩盖多维代价             | 第五章系统级度量            |
| 物理异质       | 一切负载都走同一条昂贵路径           | 第八章原语一                |

### 4.3 第一拍：一次高主频路线的终结

机制讲完了，现在来看历史是不是在偿付同一笔代价。首选案例不是计算机的胜利，而是一次失败。

2000 年代初，唯一有效的杠杆是主频。当时各种缩放规律尚未失效，把频率推上去几乎不额外支付代价——**这意味着当时的工程判断有一条清晰的、被数据支持的捷径**：只要频率还能涨，其他一切都可以先放一放。

在这个背景下，NetBurst 架构做出了它的赌注：**用极深的流水线换取高主频**。深流水线让每一级承担的工作变少，从而让时钟周期可以切得更短，频率随之抬高。这条路被一路推到了 Prescott 微架构，流水线深度加到了 31 级。

需要强调的是：**这不是一个愚蠢的设计，它是对一条仍然成立的趋势的理性外推。** 这一点与第二章说过的"由正确推理得出的错误结论"是同一个形态，而且它更干净——因为这里没有隐藏前提，只有一条看起来还会继续的外推。

代价在两端同时出现。

**前端**：分支预测失败的成本随流水线深度线性放大。31 级意味着一次预测失败要清空并重填几十级流水线，而在通用负载里，分支的密度是极高的。频率越高，每次猜错要付的时钟周期就越多——**赌注的规模越大，失误的规模同步变大**。

**后端**：功耗随频率与电压一同攀升，工艺进一步缩小时又要叠加漏电。最终的结果在微架构史上是罕见的：那颗芯片以热著称，成了行业里那个被反复引用的烫手名字。

结局在 2004 年到来：**4 GHz 型号被公开取消**。这句话值得单独停留一下——**它不是被对手打败的，是在它本该收获的那一刻被自己中止的。** 一条把全部筹码押在单一维度上的策略，通常不会死在竞争里，而是死在这条维度自身的物理边界上。

接棒的是 Core：**14 级流水线**，配合更宽的发射、更好的分支预测与更深的缓存层次，在**更低的频率**上取得了更好的性能与功耗表现。

这里必须把判读写清楚，因为读者很容易自动误读成另一个结论：**获胜的不是"更通用"，而是"更均衡"。** 深度换频率与宽度换指令级并行，两者都在设计空间里；胜出的不是某一极，而是那个把多个维度同时调到协调位置的方案。

由此得到两条教训。

**第一，主频这条单轴撞墙之后，剩下的唯一一条轴是结构。** 多核、随后的异构集成，都是那堵墙的下游产物。**异构不是设计风尚，它是物理墙的后果。** 这一条是本文全部历史正当性的来源。

**第二，赌注的形状是可辨认的。** 参数规模是 AI 的单轴。它同样是对一条成立趋势的理性外推，同样在很长时间里回报丰厚，同样会撞上第三章那三笔账。两次下注的形状完全一致——**都是把希望全部押在一个可以持续外推的单一维度上**。

但这里要克制，不能滑到"参数竞赛就是 NetBurst"这种断言上。可比的是**策略形态**，不是物理机制；两者撞的不是同一堵墙，只是都以同样的方式为同一个理由买单：**在一个方向上外推得太久，忘了系统还有别的维度。**

> **钉子：它不是被对手打败的，是在它本该收获的那一刻被自己中止的。**

### 4.4 第二拍与第三拍：异构为何不自动盈利

结构转向带来了异构。到这里，前面的论证似乎已经在暗示一个过于顺滑的结论：既然单轴撞墙了，那就多堆几种单元。而这一步必须被拦住，因为它是全文最容易被误解的地方。

异构本身不产生任何收益。

这件事有一个精确的表述方式，而且它不是一个比喻。设任务中可被加速部分占比为 p，该部分的加速倍数为 s，则整体加速的**上界**是

$$\text{Speedup} = \frac{1}{(1-p) + \frac{p}{s}}$$

这是阿姆达尔定律。它的含义很朴素：只有 p 那部分被加速了，(1-p) 那部分不受影响，而后者决定了上界——当 s 趋于无穷，加速比收敛到 1/(1-p)，再怎么投入也不会更好了。

把异构代进这个式子，会看到两件同时发生的事。

**第一件**：p 的耗时被压缩，于是 (1-p) 在总时间里的**相对权重上升**。加速越成功，协调就越成为瓶颈。这不是意外，这是定律的直接推论。

**第二件**更隐蔽，也更贵：**加入异构单元不只缩小了 p 的耗时，它还扩大了需要协调的 (1-p)。** 因为现在多了分派决策、数据搬运、跨单元同步，以及分派错误时的回退与验证。**协调本身就是工作**，而且是新增加出来的工作。也就是说，公式里那个 (1-p) 并不是一个不变的本底，它是一笔会随异构化程度一起增长的账。

于是得到一个必须写成不等式的结论：

$$\text{异构的净收益} = \text{加速收益} - \text{协调税}$$

**这个差额可以是负的。** 异构不是一笔自动盈利的交易，它是一项需要被论证的投资。这与第三章那三笔账构成一组对称：第三章算的是"统一执行"的账，这里算的是"分而治之"的账。两种做法都有成本，问题从来不是哪种免成本，而是哪种的账更容易被偿还。

现在把这套定律放回 AI 系统里，会看到一个相当不利的处境。

在传统的异构计算里，协调那部分是什么？是**廉价的标量控制码**：几条指令、一次跳转、一次总线事务。它是整个系统里最便宜的东西，所以 (1-p) 虽然存在，却无足轻重。

而在今天的 AI 系统里，协调那部分恰恰**是系统里最贵的东西**。决定下一步做什么的，是一次通用前向；工具调用的意图由概率生成，然后还要被解析出来；工具返回的结果要重新序列化进上下文，才能被下一步看见。协调的每一个环节，都在调用系统里最昂贵的那个资源。

> **我们把系统里最贵的资源，放在了那条串行路径上。**

这一句话解释了第一、三章留下的很多现象。为什么给 Agent 增加工具经常不涨反跌？因为协调税吃掉了加速收益，还顺带引入了新的失败模式——序章里那三种失败指纹（卡在推理、撞到轮次上限、陷入空转），本质上都是协调路径在膨胀时的失效形态，而不是加速单元本身的故障。

由此得到本节最值钱的一步，也是第八章的定量动机：

> **协调税必须小于加速收益。而这要求协调路径的成本与加速路径脱钩**——两者不能共用同一个昂贵资源。让决定"该谁做"的那件事，和"做"本身一样贵，是这套架构最根本的失配。

第五章的度量维度、第八章的第一条原语，动机都在这个不等式里。

> **钉子：异构本身不产生收益，它只是把收益的可能性摆上桌；兑现它的东西，另一个人。**

### 4.5 第四拍：收益在哪里兑现

如果异构的收益不是异构单元产生的，那它是谁产生的？

答案很直接：**它由分派决策产生。** 加速器能做的只是放大"做对了之后"的收益；决定"这件事该谁来做"的那个东西，决定收益究竟存不存在。分派错了，加速单元跑得再快也只是更快地做了不该做的事。

这就带来一个定位上的调整，而且是本章最重要的一次调整：

> **调度不是实现细节，它本身就是价值兑现层。**

这条定位让第八章的第一条原语从"一种必要的纪律"升级为"收益的所在地"。纪律是可以可选的美德，价值所在地不是。

第二件随之而来的事，是契约。异构单元越多、越可替换，把它们与上层绑死的代价就越高。指令集与微架构的分离给出了这个结构最干净的形态：同一份程序可以跑在完全不同的硬件实现上，因为中间存在一份稳定的契约——**契约层的价值，随下层的可替换性增加而增加。** 下层换得越勤，把上层钉死在下层上就越昂贵。

把这条映射回本文的处境，会得到一个比历史类比更硬的东西：**模型换代越快，"把应用绑死在某个具体模型上"的债务就越高。**

于是序章里那个"每个模型都要单独定制一套脚手架配置"的观察，从这个角度看就不再是一项烦人的工程杂务，而是一笔持续累积的债：它的利息由下层的迭代速度决定，而这个速度正在加快。**下层跑得越快，缺一份契约就越贵。**

对照组也很清楚：硬件工业用了几十年，把这件事标准化成了层层接口——指令集、驱动模型、总线协议。而 AI 直到今天，连一份都没有。

> **钉子：价值不在哪一件部件里，在决定把它送到哪里去的那件事里。**

### 4.6 收束：无边界不是稳定解

三点观察可以收束本章：**无边界从来不是稳定解；边界会移动；而移动的方向由机制决定，不由偏好决定。**

第一点已经由前四拍给出——不是因为我们打比方打了三次，而是因为三条机制与领域无关。第三点则是本章反复出现的纪律：机制决定了单轴撞墙之后必须有结构，结构带来协调，协调要求成本脱钩，成本脱钩要求分派权独立出来。这条链条上没有一步是"因为我们希望这样"。

剩下的第二点，需要回到第一章那个岔路口。

**吸收与固化，是两股方向相反的力量。** 模型变强，会吸收掉一部分原本在外部实现的职责，这一点第二章已经用一张表确认过。但同时，越来越多负载的规律正在被锁定，它们从"探索侧"迁往"执行侧"，从而需要被固化。所以"模型会不会吃掉 Harness"这个问法本身就不够精确，该问的是：**它会吃掉哪一部分。**

判据在 2.4 就给过了：**能在一次前向内部闭合的会被吸收，不能的会固化。** 这里补上一条动态的观察，它也是执行层不会因为模型变强就失去存在理由的根本原因——**这条边界不是固定的，它在持续漂移。** 总有新的负载被锁定，因此总有新的东西需要被固化；吸收与固化同时在发生，方向上对冲，但谁都不归零。

历史给过一个形状非常好的先例。外置浮点协处理器最初以外挂形式出现，后来被整合进处理器内部。但**被整合不等于被取消**——"专门计算由专门单元承担"这条原则从未被推翻，被重新画的只是它的边界。这正是岔路口 (b) 的形态：吸收与安置并不矛盾，它要求的是**被吸收之后剩余的那部分被妥善安置**。

到此可以正式确立本章的隐喻：

> **Transformer is the CPU, not the Computer.**

它表达的是一件很朴素的事：通用语义处理是一件了不起的能力，它值得拥有一个完整的体系围在它周围；但它自己不是那个体系。

它**不**表达的是：不表达"Transformer 会被淘汰"，也不表达"应当降低它的地位"。恰恰相反——CPU 是计算机里最通用、最不可替代的部件。**这句话是在抬高它，不是贬低它。**

而如果再加上 4.4 的结论，这句话还有一层更贴切的读法：既然收益由分派决策兑现，那么这台机器的价值同样不在核心里。**价值在机器里，不在核心里。**

> **钉子：层级不会被取消，它只会被重新画。**

### 4.7 过渡

结论可以交给下一章了。既然系统的成败由多个部件共同决定，而这些部件之间的分工不是偏好而是必然，那么孤立地度量其中任何一个部件，就已经不再具有工程指导意义——它测的仍然是一个真实的东西，只是那个东西已经不再是决定结果的那个东西。

第五章要做的，就是换掉这把尺子。

## 5. 评价维度的迁移 —— 从分数到剖面

第四章的结论留下了一个尴尬的处境：既然系统的成败由多个部件共同决定，而这些部件之间的分工不是偏好而是必然，那么孤立地度量其中任何一个部件，就已经不再具有工程指导意义。它测的仍然是一个真实的东西，只是那个东西不再是决定结果的那个东西。

这一章要换掉那把尺子。

先声明本章交付什么：一份报告规范，回答"当你报出一个数字时，还欠读者什么"；四个维度及其测量方法；以及一条接口——度量与调度共享的那套负载特征。

也声明本章不做什么，因为这份声明同时也是它的姿态：**本章不设计榜单，不新增数据集，不提出新的评分公式，也不提供一个"系统总分"。** 它不打算告诉大家什么系统是最好的，只打算说清一件事——一个分数在被报出的时候，它到底携带了多少信息，又丢掉了多少。

### 5.1 单变量度量的破产

先不要说旧度量错了。它错了这句话既不公平，也说不清楚。

单变量分数之所以能统治这么多年，是因为它做对了一件很具体的事：**它让研究变得可复现。** 固定提示、固定模板、固定输入格式，于是变量只剩下一个，谁跑都一样。在实验室条件下，这是唯一正确的选择——不这样做，任何改进都无法被确认。

问题出在这个数被搬出实验室之后。所以准确的表述不是"分数不准确"，而是：

> **分数不是模型的函数，它是配置的函数。** 当我们写下"某模型在某基准上得分 X"时，我们实际测量的是一组配置的联合输出；而报告只留下了其中一个变量名。

这个视角一旦立起来，序章里的那些"异常"就全部不再异常了。同一个模型换一套脚手架，分数跨度可达数倍——它们从来都在那里，它们只是**同一个函数在其他自变量取值上的采样**。函数一直是多元的，只是我们长期只报一个点。

这里必须接着第三章的立场说清一件事，否则本节很容易写歪。第三章已经确认：Agentic 系统完全可追踪，每一步的工具调用、每一次上下文修剪、每一轮重试都被完整记录；从外部重新配置也完全可行，换工具、改提示、调重试策略都不需要动模型。所以本章**不能**诉诸"无法归因"——那个说法已经被撤销了。

正确的表述是：**归因的能力一直存在，被浪费的是它留下的东西。**

一次调优所得的经验之所以无法迁移，不是因为没人看清发生了什么，而是因为**没有一种格式承接它**。它被记在哪？记在一份没人读的日志里、一次内部会议的结论里、某个人对某个模型的直觉里。它并非不可知，它只是不可引用——而没有格式可以引用的东西，无论被看清多少次，都不会累积成资产。这与序章里 Skills 带来的那种"可见性"是同一件事的两种说法：**一旦某样东西必须被具名，它就再也无法被当作不存在。**

于是本章的动机可以一句话说完：**报告规范就是让配置成为一等对象的制度形式。**

重新平衡一下此前那种说法的话，可以这样表述：配置既是归因的主体，也是性能的主体；它之所以一直被称为"基础设施"，只是因为从没有人把它当作结果来报告。

### 5.2 报告规范：让配置成为一等对象

下面这张表是本章的第一件产品。它要回答的正是 5.0 提出的那个问题——报出一个数字时，还欠读者什么。

| 字段                             | 为什么必须随分数一同出现                                     | 缺失时会发生什么                         |
| -------------------------------- | ------------------------------------------------------------ | ---------------------------------------- |
| **模型与版本**                   | 旧规范已有的唯一字段                                         | ——                                       |
| **Harness 及其版本**             | 它承载了评估中真实发生的决策：工具选择、上下文管理、停止策略 | 分数不可复现；同一模型的两份报告无法比较 |
| **配置档（Scaffold Profile）**   | 同一模型的不同配置，是两个不同的系统                         | 差异被合并进"模型能力"，产生系统性高估   |
| **计算预算**                     | 成本本身就是效能的一部分；预算不同则分数不可比               | 达到同一分数的代价差异被完全隐藏         |
| **终止与重试策略**               | 它决定"什么时候算做完"以及"错了给几次机会"                   | 完成率失去意义：放宽终止条件即可抬分     |
| **计分准则**                     | 格式正确与任务正确是两件事                                   | 表面合规被读作真实成功                   |
| **可靠性（重复次数与结果散布）** | 诊断依赖实验，而实验噪声常与效应同处一个量级                 | 把噪声读成改进，或把改进读成噪声         |

这张表容易被误读成"给现有榜单加几个标签"。所以要把它的性质说准：**一份没有配置说明的分数，不是一个不完整的分数，而是一个无法参与比较的数字。**

这两种说法的差别很大。前者意味着"信息少了点，但还能用"；后者意味着"这个数字在缺少同伴的情况下不承载结论"。这与测量学里那句老话是同一个道理：没有不确定度的测量不是一次粗糙的测量，而是**根本不成其为一次测量**。

第七个字段值得单独强调，因为它是本章新增的要求，也是最容易被以"太贵"为由砍掉的一个。它要处理的是这样一种情况：一次改动带来的提升，恰好落在同一配置重复运行的散布范围内。此时单次结果无法区分"我改对了"和"今天运气好"，而唯一的区分办法是把散布报出来。所以它不需要每个场景都做高重复——**只有当改进的幅度接近噪声水平时，报告散布才是必需的**，而恰恰是那个时刻，人们最容易忘记它。

第六个字段则承接的是旧度量自身就有的缺陷：把格式合规当成任务成功。它是旧账，不是新债。

把这张表压缩成一个形式化的示意，能让它的必要性变得直观。设想两份真实存在的报告：

```
配置档甲（脚手架 H₁ / 终止策略 S₁ / 预算 B₁）  →  得分 X
配置档乙（脚手架 H₂ / 终止策略 S₂ / 预算 B₂）  →  得分 Y
─────────────────────────────────────────────
旧规范的输出：  某模型在某基准上得分 —— 保留其一
```

X 与 Y 之间的差距，正是序章里那两行记录的样子——同一个模型，从 52.8 到 66.5。**而在旧规范下，这两行会被合并成一行，合并过程中被销毁的是信息的全部来源。** 这不是报告的疏漏，这是报告的**设计**：它的目标是给出一个可排序的数，而排序要求把配置差异当作噪声抹平。

最后要补一句公平话，因为它同时是这项规范的辩护。第二个字段会让闭源配置的分数无法通过第三方验证，因此也无法被公允地承认——这看起来是在为难厂商，其实相反：**一份完整的配置说明让成绩可被信任，而不是让成绩变大。** 在旧规范下，一个不可复现的高分与一个可复现的高分在名义上等值，而实际上前者更占便宜；这既对复现者不公，也对认真做配置的厂商不公。规范是对双方的共同保护。

> **钉子：报告不是给分数加注释，而是承认分数从来不独自行走。**

### 5.3 四个维度与它们的测量方法

有了报告格式，还要回答报什么。以下四个维度各自按同样的四段来写：**定义、测量方法、它抓到什么别人抓不到的、它最容易被误用成什么。**

最后那一段不能省。一个没有写清误用形态的指标，等于把博弈的入口留给了下一个人。

#### 维度一：成败与鲁棒性

**定义**：不只是任务完成率，还包括同一配置下重复运行的结果分布，以及失败按发生位置的分类——是决策环节、工具环节，还是终止环节。序章里那三种失败指纹（卡在推理、撞到轮次上限、陷入空转）已经示范了这种分类的形态，此处不再展开。

**测量方法**：固定配置多次运行，报告分布而非单点；失败按位置归类，而不是只记一个"失败"。

**它抓到什么**：区分"偶尔做对"与"稳定做对"。这两件事在旧规范里是同一个数字，而它们在工业场景里的价值不同——后者才可以被依赖。

**它最容易被误用成什么**：**优化终止条件本身就能抬分。** 一个系统可以通过更早宣布自己成功来提高完成率，而这件事完全不需要它变得更强。这也是为什么"终止与重试策略"必须是报告字段：指标与它的防线必须成对出现，否则一个好的指标会变成一条捷径。

#### 维度二：单位工作的物理代价

**定义**：完成一件实际任务所支付的总代价，而不是单次调用的开销。

**测量方法**：以**任务**为摊销单位，统计算力消耗、内存与显存搬运、上下文峰值占用，以及端到端延迟。核心操作是把"每 token 成本"换成"每任务成本"——前者可以很漂亮，而任务又长又绕。

**它抓到什么**：**它把"用更多轮次换分数"这条捷径摆到台面上。** 序章那个"同一任务消耗量相差数十倍"的观察，只有在这个维度上才显现为一个问题；在单次调用成本上看，两侧几乎一样。

**它最容易被误用成什么**：只看平均值而忽略尾部。少数极贵的任务往往决定系统在工业上是否可用，而平均数会把它们抹平。

#### 维度三：协调开销比

**定义**：整任务的成本中，**协调路径所占的比例**。凡是属于分派决策、上下文搬运、意图解析、结果重新序列化的开销，都计入协调侧；其余计入执行侧。

这是本章相对大纲新增的维度，也是第四章那笔债的直接偿还。4.4 给出的不等式——净收益等于加速收益减协调税——在这里第一次变成可观测的量：**协调税从一个分析性概念，变成一个可以逐次追踪的指标。** 它的用途不是核算，而是预警：如果协调占比在持续上升，说明加速收益正在被吃掉，而不必等到系统崩掉才发现。

**测量方法**：按"做"与"决定该谁做"切分执行过程，两侧分别记账。

这里要老实交代它的精度边界。在有些架构里，这条切分线是模糊的——一次上下文重写既是执行的一部分，也可以被看作协调的一部分。所以它**目前是一个趋势指标，而不是一笔可以入账的精确核算**。这不妨碍它有用：它不需要精确到能被审计，只需要能显示比例是否在单调上升。（至于它将来能不能被精确测量，很可能可以——只要协调路径与执行路径在物理上分得开，这条账就自然分得开。而这本身就是一个值得追求的设计目标。）

**它抓到什么**：它是四个维度里唯一能反映**分派质量**的一个。没有它，一个调度得很差的系统和一个调度得很好的系统会报出同一个分数——两者的差异全部藏在被抹平的配置里。

**它最容易被误用成什么**：把它当成一个纯粹的效率指标去最小化。协调不是纯粹的浪费，**零协调的系统根本无法协同**。要的是比例合理，不是比例为零。

#### 维度四：可确知性与自愈

**定义**：两个面向——关键状态可否被确定性地重放，以及失败后可否在没有人工介入的情况下恢复。

**测量方法**：能否对两次运行的中间状态做快照比对；在明确注入故障的条件下，无人工干预的恢复率与恢复耗时。

**它抓到什么**：**它度量的是第三章那个"特权级"是否存在。** 一个能被打断、能被回滚、能被重放的系统，在这个维度上得分；一个只能靠"再问模型一次"来恢复的系统不得分。这是四个维度中唯一直接反映体系结构属性、而不反映调优水平的一个。

**它最容易被误用成什么**：把"重试成功"等同于"自愈"。重试只是重新采一次样，自愈是把状态修回一个已知的良好点。这两件事在成本与可靠性上完全不同——**把重试算作自愈，等于用一个采样过程冒充一次状态修复**——因此它们应当分别报告，不能合并。

#### 四维对照

| 维度             | 测量单位           | 抓到什么             | 主要误用形态     |
| ---------------- | ------------------ | -------------------- | ---------------- |
| 成败与鲁棒性     | 任务分布（非单点） | 稳定做对 vs 偶尔做对 | 放宽终止条件抬分 |
| 单位工作物理代价 | 每任务             | 用轮次换分数的捷径   | 只看均值忽略尾部 |
| 协调开销比       | 成本占比           | 分派质量             | 追求零协调       |
| 可确知性与自愈   | 重放可比 / 恢复率  | 特权级是否存在       | 重试冒充自愈     |

四个维度彼此不可通约，任何把它们合成一个数的方式都是一次立场声明。这一点是下一节的前置条件。

> **钉子：度量一个系统，就是承认它同时欠着四笔互不相抵的账。**

### 5.4 度量与调度是同一件事的两面

本节很短，但它是这一章结构上的枢纽。

要说的是一句对称命题：

> **你无法调度一个你无法刻画的东西；而一个能用来刻画负载的特征，恰恰就是它应当被报告的那个指标。**

两个方向各说一句。

**从度量到调度**：第三章交付的两条判据——刚性谱与信息密度——在这里第一次合流。它们既是"这个负载该走哪条路径"的路由依据，也是"这个系统该被如何评估"的刻画方式。**同一组特征，既输入调度器，也输入报告。** 这解释了为什么本章不是一篇关于评测的文章：它不是站在外面评价系统，它给出的特征集本来就是系统内部要用到的东西。

**从调度到度量**：第四章说过，异构的收益由分派决策兑现。若如此，度量就必须能反映分派质量——这正是维度三存在的理由。反过来说也成立：一个无法显示分派质量的度量体系，无法指导任何调度决策，因此它对运行时设计没有价值。

由此得到一个结构性判断：**度量与调度共享一套特征，所以它们必然由同一层来承担。** 这也解释了为什么第八章那几条原语不能各自独立地长在应用层里——它们注定要共享同一套负载刻画。此处只作标记，不展开。

### 5.5 抗博弈：为什么必须多维

这一节是第四章机制二在度量层的兑现，也是全章唯一一处机制式论证。

前提是一条经验规律，不是道德指控：**任何决定资源分配的度量，都会被优化。** 凡是能被看见的东西，就会有人去改善它的读数，这与诚实与否无关，与激励结构有关。

推论随之而来：**单一标量分数必然被博弈。** 因为对任何单一目标，总能找到一种廉价的、不改善真实能力的方式去抬高它。这在旧度量里表现为格式迎合与模板过拟合；到了系统层，博弈的对象换成了终止策略、预算口径与协调比例——形态不同，机制同一。

于是机制二在这里的直接应用是：**维度不可通约，恰恰是抗博弈的必要条件。** 一个维度被抬高，另外三个维度会显示它的代价。这就是为什么 5.2 那张表里的终止策略既是维度一的防线，也是维度一的一部分。

但这里必须紧接着处理一种很常见的误读：**抗博弈不等于增加维度数量。** 堆叠指标不解决任何问题，因为多指标同样可以被加权成单一标量，而加权一旦固定，博弈就会重新集中到那个标量上——只不过现在它要同时照顾几个方向，于是它学会了一种更精巧的作弊方式。

真正的防线是**保留不可通约性本身**：不给出统一的加权方式，让每一次评价都显式声明它此刻优先什么。

这也顺手说明了本章最重要的那处克制：**本章不提供一个"系统分数"。** 这不是未完成的半成品，而是刻意的——把系统效能也压成一个标量，就是重演一次同一个错误，只不过这次被压缩掉的东西，正是我们刚刚费了两章才看清的那些维度。

> **钉子：多维不是繁琐，而是让分数无法被廉价的努力收买。**

### 5.6 收束：从分数到剖面

写到这里，可以回头看一眼序章里那个词。

"Scaffold Profile"——每个模型都要单独定制一套脚手架配置。它被发明出来的时候，是一个工程杂务的名字，是调参调到精疲力竭时的一句抱怨：**又得为这个模型重新调一遍。**

但它描述的那个东西，恰恰就是应该被报告的对象：一个模型在特定外围条件下的完整工作形态。**行业其实早就给出了正确的答案，只是没有把它当作度量对象。** 答案一直在场，它只是穿着工装。

所以本章的结论应当用一句形态描述来收，而不是用一句呼吁：

> **度量单元从"模型"迁移到"系统"，其具体形式不是从分数变成更好的分数，而是从分数变成剖面。**

剖面不是一个更全面的分数。分数是投影，剖面是那个被投影的东西本身；投影可以比原物体更好看，但它永远无法承载一个原来就被丢掉的方向。

顺带可以看清一件事，它是本章对全文主题最有价值的一次反向推进。第一章说过，Harness 没有形状——它是一堆没有边界、没有契约、互相不识的约定俗成。**而一旦配置必须被报成一份剖面，没有形状的东西就无法被提交。** 你没法为一件说不清自己是什么的东西写一份字段完整的报告；你必须先给它分层、给它命名、给它边界，才写得出那七个字段。

也就是说：**度量方式的改变本身就是一种压力，它迫使抽象层出现。** 这是本文全部论证里唯一一条不靠算力成本、不靠工程效率、而靠"必须交代清楚"来推动变化的力量。它比前几章那些账都更温和，但可能更有效——因为它的施加者是每一个需要引用别人结果的人。

现在我们大致知道该测什么、该报什么了。但还有一个更靠前的问题始终没有回答：既然需要的是一个系统，那么已经存在的那些框架为什么不算？

下一章正面处理这个质疑。

## 6. 为什么 Agent Framework 不是操作系统

第五章结尾留了一个问题：既然需要的是一个系统，那么已经存在的那些编排框架为什么不算？

这个问题必须先回答，否则本文之后的一切都只是一份库的设计倡议——你抱怨现有方案不够，然后提出一个自己的方案，这在工程界每星期都会发生几次。

也先说清本章的对手是谁。它不是某一个实现，而是**这一类东西的结构共性**。挑软目标来打是最容易的自我取悦：任何一个广被使用的框架，都有它具体的粗糙之处，指出这些粗糙既不需要洞察，也无法支撑结论。所以本章讨论的是那个类别本身——**它们共有的位置、共有的抽象方式和共有的边界**。

于是本章要问的不是"框架做得够不够好"，而是：**它站的位置对不对？**

这两个问题看起来差不多，差别却是全部：前者可以靠时间解决，后者不能。功能可以一直加，而位置不会因为功能变多而改变。一个不断加功能的库，最终得到的是一个功能更全的库。

### 6.1 对手的最强版本

要讨论一类东西，得先把它的最强版本说出来，而且要说得比它自己的拥护者说得更好。有四件事必须承认，一件都不能吝啬。

**它解决了真问题。** 第三章说过，模型的边界之外是一片荒原——没有坐标系，没有公共道路。而框架是这片荒原上最早出现的私有道路。在没有人提供地基的时候，它给了可以通行的路线，让大量团队得以把东西真正交付出去。这份功绩不该被抹掉，也不该被稀释成"临时的胶水"。

**它把若干不可省略的实践固化了。** 工具调用该长什么形态、一次执行该被怎样记录、会话状态该被如何拼装——这些在它手里第一次变得可复用。在此之前，每一处都是各自摸索。

**它的生态位是真实的。** 它站在应用侧，因此对应用开发者的痛感最敏感，响应也最快。这让它成为过去几年里最有效的实用主义载体。

**最后一条最关键——它最强的反驳来自本文自己的论断。**

> "你在第一章说过，Harness 是智能计算的主体；而我们的框架就是 Harness 的实现。既然如此，你为什么还要说缺一层系统？"

这是全章唯一必须正面回答的质疑，因为它引用的是本文此前建立的结论。回答需要两处切分。

第一处切分，是那个已经被行业自己写出来的等式。学术讨论早已给出：**Agent = Model + Harness。** 这个等式是对的，而且它承认了一件了不起的事——一个可交付的智能体，其能力由模型与外围共同构成，而不是由模型单独决定。本文全文都建立在它的结论之上。

但请留意这个等式的**性质**。它是一个关于"产物由什么组成"的等式，不是一个关于"谁统治谁"的等式。加号的两侧地位平等，**等式没有说明谁是主语。** 而在每一份现有实现里，这个空白都被同一个答案默默填上了：模型是主体，外围是它的附属。

> **这个等式没有说谁说了算；而所有现有实现都默默地填了同一个答案。**

第二处切分随之而来。既然 Agent = Model + Harness，那么 Agent Framework 就是**这个等式里 Harness 那一项的一种实现方式**。这是一步重要的降格：它是一个**变量的取值**，不是一个**层级**。它回答了"Harness 可以怎么写"，没有回答"Harness 应该坐在哪里"。

于是第一章那句论断与本章并不冲突，而是拼成了一个完整的诊断。第一章说的是，外围计算已经实质性地承担了智能；本章说的是，**承担智能的那些计算没有自己的地基。** 框架不是错的东西，它是**在一块没有地基的地上，搭得比较好的那一间房。**

所以质疑的形式应当被改写。原来问的是"框架算不算系统"，正确的问题是可回答的那个：**框架能不能被放到底座的位置上？**

### 6.2 Agent 是一个捆绑名词，不是一个层级

从最便宜的一层切入，但结论比它看起来重要。

"Agent"这个词同时指代了好几件东西：一个模型、一套策略、一个状态容器、一个执行循环、一组工具、一份权限范围。这在日常交流里不是问题——大家心照不宣。问题在于它同时也在架构里被这样使用。

它是一个**会话级的名词**，不是**层级级的抽象**。后果有三。

**它无法被组合。** 能被放进架构里的东西，必须能被放在某一层上，且只承担那一层的职责——这样它才能被别的层调用，也才有资格调用别的层。一个横跨所有层的名词，无法被谁调用，也无法调用谁，它只能被整体使用或整体替换。

**它无法被版本化。** 你没有办法说"我升级了 Agent 的调度部分"，因为没有那个部分；你只能说你换了 Agent。这与第三章那笔累积税是同一件事的另一种记法：经验之所以无法沉淀，是因为它没有可以挂靠的部件。

**它的配置面暴露了它的世界观。** 这一条留到 6.5 用，先在这里埋下：看一份配置列表，就知道它认为世界的中心在哪里。

还要接上第五章的一处结论。5.6 说过，一旦配置必须被报成一份剖面，没有形状的东西就无法被提交——你没法为一件说不清自己是什么的东西写一份字段完整的报告。**框架之所以报不出剖面，不是因为它不愿报，而是因为它没有可命名的部件。** 它只有"一个 Agent 加一套配置"。

名词问题因此不是术语洁癖。**一个不能被放在某一层上的东西，无法成为契约的另一端**——而契约正是第四章机制一给出的那个答案。

### 6.3 根本错位：整套抽象都是站在模型那一侧写的

现在到了本章的正文。这个诊断有两个侧面，它们说的是同一件事。

#### 6.3.1 谁是宿主，以及一次运行由谁持有

先把定义上的差别说清：库与运行时的区别不在功能，而在**它在栈里的位置**。库被调用，然后返回；运行时一直在场，并由它决定何时进入别的代码。

这条差别直接解释了第三章那片荒原。若控制循环住在应用代码里，那么它**不可能被共享**——不是没人愿意共享，而是它根本不在任何一个可以被共享的位置上。N 个应用各自持有一个循环，正是那笔"重造税"的机制来源。

那么为什么模型不能当宿主？这里不需要"没有人认为操作系统跑在 CPU 里"这类类比，可以给一条实质标准：

> **宿主必须拥有两样东西：时间和记忆。** 它得知道现在走了多久、还剩多少预算、刚才发生了什么、哪些步骤已经完成。而模型两样都没有——**它的记忆是别人交给它的，它的时间只在被调用时存在。**

一个连"现在几点"都不具备的东西，无法持有控制权。它可以是极强的一次计算，但它不能是那个决定"接下来做什么、还要不要继续"的角色。

而且这两样东西不是抽象的美德，它们是一个宿主**用来做具体事情**的：知道还剩多少资源，才能调度；知道状态在谁手上，才能中断；知道哪些步骤已完成，才能销毁一次不该继续的执行。**所以宿主资格的具体行使，就是对一个运行实体的生命周期管理。**

把这一点转到正面，就得到了应有的定位：在一个真正的系统里，语言模型应当成为**系统可以调用的、通用性最强的一件语义认知组件**——注意是"组件"，不是"宿主"。系统可以调用它，可以调用别的组件，也可以把同一件事分给几件组件共同完成。

这里要立刻说明这不是贬低。**把它从"唯一的大脑"改成"最强的组件"，降低的是它的管辖范围，不是它的地位。** 一台机器里最通用、最不可替代的那个部件同样是组件——第四章那句"Transformer is the CPU, not the Computer"，说的正是这件事在此处的形态：CPU 是组件，而组件是被系统调用的。

接下来是更关键的一步：**系统要能拉起一次认知运行、给它派活、给它配额度、在它跑偏时中断它、在它完成或无望时销毁它。**

请注意被管理的对象：**是"运行"，不是"组件"本身。** 权重不需要被销毁，它是一份可以被加载的资源；被创建、被计量、被销毁的，是这次加载之后的**那一次执行**。用既有系统里的话说：**被调度的是进程，不是可执行文件。**

这条区分看似是术语问题，实际是全部问题的所在。今天，模型调用与任务在外延上**是同一件事**——任务从第一次调用开始，到模型不再说话结束；调用的生命周期就是任务的生命周期。于是出现了一个无解的结构：**你无法站在任务之外结束一个任务，因为在那个位置上没有别的东西。**

这就把第一章那桩悬案彻底结掉了。终止语义之所以只能被写成提示词里的一句礼貌请求，不是因为它难，而是因为**当运行的边界由模型说到哪里来划定时，除了请求之外没有第二种终止方式。** 一个没有被系统定义过边界的执行，可以被挂断，却无法被中断——挂断之后状态烂在那里，中断则要求有一个已知的良好点可以回退。

> **模型今天不是一个可以被打断的东西，它只是一个会被挂断的东西。**

这条区分的后果有三重，都指向后面的章节。

**能被中断，才有可确知性。** 这直接呼应第五章的第四个维度：状态之所以无法重放，前提是运行没有系统定义的边界——**边界不在，快照就无从谈起。**

**能被计量，才能被报告。** 第五章要求以"任务"为单位摊销成本、按"做"与"决定该谁做"切分开销；而一个任务要能作为计量单位，前提是它有系统定义的起止。**因此那套度量体系隐含地要求这里定义的运行边界。** 这一点值得留意：它是一处回环，度量需求在倒逼运行语义。

**能划边界，才能配额。** 多个并发运行共享同一份权重与同一批算力时，"配额"才有意义。此处只作标记。

于是控制反转的正确表述不是"把方向盘从模型手里抢走"，而是：**让模型第一次拥有一个可以在其中被管理的存在形态。** 它不再是一段永不结束的对话，而是一次可以被创建、被计量、被打断、被回收的执行。

#### 6.3.2 它把模型的语言抄成了自己的接口

另一半是本节真正的原创部分。

框架提供的抽象词表，是**模型世界的词表**：消息、角色、回合、工具调用、上下文。这些词描述的是"模型怎么说话"，而不是"系统怎么工作"。它们全都以一次对话为参照系——包括那些看起来很像系统词汇的东西。

关键在于抽象的一个基本性质：**一个用被抽象者的语言写成的抽象，其稳定性上限就等于被抽象者的稳定性上限。** 而模型正是整条栈里变化最快的那一层，于是这层抽象只能跟着一起变。"每个模型一套配置"的深层原因在此：不是配置需要重写，而是**抽象本身随着它所描述的对象一起漂移**。

这同时也是第四章那条契约层论证在此处的具体落点。契约之所以值钱，是因为它**不与两侧共变**——4.5 说契约层的价值随下层的可替换性增加而增加，而一个抄着下层语言的契约，其可替换性是零。

反面对照不难构造。一套真正与模型无关的词表，用的会是另一批词：**任务、资源、约束、状态、预算、中断**。这些词在模型换代时不会改变含义，因为它们描述的不是某一方怎么说话，而是系统要办什么事。

把两半合起来，就是本章的诊断：

> **整套抽象都是站在模型那一侧写的。**

这句话涵盖了两件事。词表这一半说的是：**接口是站在模型那一侧写的**——连系统自己的概念也用对话的语言表达。生命周期那一半说的是：**组件没有可管理的存在形态**——因为从没有人从系统那一侧定义过它，运行的边界只能由被管理者自己划定。同一个姿势，两种表现。

它也顺手给出了第七章那个定位的前提：**重新定位模型，前提是先有一套不按模型写的词。** 否则无论怎么调整模型的地位，词表都会把它重新推回中心——因为在一个以对话为中心的词表里，只有模型有台词。

### 6.4 三条现场判据

把前面的论证转成可以当场使用的问题。三条判据各自要挂一笔已经算过的账，否则它们只是重述。

**判据一：控制循环住在谁的仓库里？**

问法：那个决定"下一步做什么、要不要继续"的循环，是在你的应用代码里，还是在某个你不需要自己写的位置上？

挂靠的代价：第三章那两笔税。若它在你的仓库里，则重造税与累积税在这一刻就已经发生——**循环不可共享，因此它的每一次改进都是本地的。**

使用方式：看循环住在哪，不看框架提供了什么。

**判据二：一次认知运行有没有系统定义的边界？**

问法：这次执行是谁拉起的？谁给它配了额度？它跑偏时谁能中断它？结束后谁来销毁它、回收它的状态？

挂靠的代价：1.3 的终止语义悬案、3.2 的特权级、5.3 的第四个维度。**一个只能被挂断、不能被中断的执行，不可能真的持有控制权**——它只是在被允许的范围内前进。

这里也顺带分清一对常被混用的词：**超时是外部掐断连接，中断是系统在不变式完好无损的前提下收回控制。** 前者会让状态烂在那里，后者不会。

**判据三：违反契约时会发生什么？**

问法：一个不合规的输出交到系统手里，是被拒绝，还是被解析并尝试挽救？

挂靠的代价：5.2 的第六个字段（格式合规被读作任务成功），以及 1.2 那个判断——结构化输出是这批补丁里唯一一次"选对了地方"的修补，因为它把约束交给了基底强制执行。**其余各处，规矩都还停留在期待。**

三条判据有一个共同的形态，值得一句话概括：它们问的都是**权力**，不是**功能**——谁持有循环、谁定义边界、谁负责执行约束。

也不需要读完一个框架的源码，只需要回答三个是非问题。**三条里一条也答不出"是"的系统，无论功能多全，都还站在空缺的那一侧。**

### 6.5 对比总表与三种辩解

把前面的论证整理成对照，六行。需要注意这不是能力清单，而是**位置清单**——每一行问的都是"这件事由谁负责",不是"这件事做得好不好"。

| 维度             | 编排框架（以模型为中心）               | 运行时（以系统为中心）               |
| ---------------- | -------------------------------------- | ------------------------------------ |
| **控制主体**     | 模型的解码循环                         | 内核的调度循环                       |
| **状态载体**     | 易失、扁平、按顺序拼接的序列           | 可寻址、有生命周期、可独立引用的状态 |
| **通信机制**     | 自然语言与启发式解析                   | 明确契约与事件通道                   |
| **计算分配**     | 单一模型承担全流程，工具作为外包函数   | 按负载特征路由到不同性质的计算单元   |
| **容错与确定性** | 只能被挂断，重新采样再问一次           | 可以被中断，支持隔离、回滚与重放     |
| **经验累积**     | 绑定在"模型＋配置"上，换掉任一项即失效 | 以契约与策略的形式沉淀，可跨下层替换 |

这张表的一处细节值得单独指出：第五行那对词——**只能被挂断 / 可以被中断**——是整个表格里最能一眼看出分野的地方。它也不是修辞：6.3.1 已经说明，这两种状态对应的是有无系统定义的运行边界，而边界的有无会一路决定可确知性、可计量性与配额能力。

而这张表还有一层隐含前提需要说明：**它的每一行都以"存在一次有边界的运行"为默认前提。** 换行去看那六行的对比，其实都在描述同一件事在不同侧面上的表现——谁是宿主、状态归谁、规矩由谁执行。这也解释了 6.2 那个名词问题的代价：一个横跨所有层的名词，在这六行里找不到自己的位置。

现在处理三种残余的辩解。每一种都要短。

**"框架会成熟起来，功能补齐就好了。"**

成熟是在纵向上加功能，而控制反转是**位置变化**。位置不会因为功能变多而改变。这恰恰是本章存在的理由：如果问题只是功能不足，本章根本不必写。

**"这只是命名之争吧？"**

若只是名字的问题，就没有必要为每一个模型重配一遍整套外围——序章里那个观察，以及第三章那笔累积税，都是这笔成本的账单。**代价本身就是命名问题的证据。**

**"那你们要做的，不也是一个框架？"**

这是三种里最危险的一问，也是本章唯一不在这里回答的一问。**差别不在新旧，而在是否站在那个空位上。** 而这句话本身还不是答案，只是一个判据；完整的回答取决于那一层究竟是什么、必须沉淀出什么——那属于后面两章。

（顺带回收 6.2 埋下的线：看一份配置列表，就知道它认为世界的中心在哪里。当配置是以"换哪个模型"为主轴组织的——每个模型一套提示、一套工具形态、一套重试策略——它其实自白得很清楚。）

### 6.6 收束

本章的结论可以压回一句形态描述：

> **缺的不是又一个胶水库，而是一个位置。** 而那个位置需要有宿主资格的东西来担任，**宿主不能由被宿主的东西来担任。**

与第一章的关系也在此处闭合。第一章证明的是，外围计算已经承担了智能；本章证明的是，**承担智能的那些计算没有自己的地基。** 两者合起来，才是完整的诊断——不是"Harness 错了"，而是"**Harness 是对的，它只是没有地基**"。

至于那个位置坐上去之后要做的第一件事，本章也已经给出了：**给认知运行画一条边界。** 有了边界，才有计量、才有中断、才有回收；没有边界，最礼貌的终止方式也只能是一句请求。

现在我们知道缺的是一层，也知道它必须坐在模型与组件之间，知道它的词表不能再抄模型的语言。下一章给出这一层的名字、定义，以及它应有的位置。
